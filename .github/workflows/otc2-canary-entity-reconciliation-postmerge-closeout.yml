name: OTC2 Canary Entity Reconciliation Post-Merge Closeout

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened

permissions:
  contents: write

concurrency:
  group: otc2-canary-entity-reconciliation-postmerge-closeout
  cancel-in-progress: false

jobs:
  closeout:
    if: github.head_ref == 'docs/OTC2-20260803-canary-entity-reconciliation-closeout'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact closeout branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: docs/OTC2-20260803-canary-entity-reconciliation-closeout
          fetch-depth: 0

      - name: Persist blocked parent checkpoint and remove temporary closeout files
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/otc2_canary_entity_reconciliation_postmerge_closeout.py
          rm -- \
            .github/scripts/otc2_canary_entity_reconciliation_postmerge_closeout.py \
            .github/workflows/otc2-canary-entity-reconciliation-postmerge-closeout.yml
          git add -A -- \
            .github/scripts \
            .github/workflows \
            docs/agents/tasks/active/OTC2-20260803-playability-p2-canary-world-protocol.md \
            oteryn-client/docs/evidence/playability/p2/canary-current-runtime-baseline.md
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'docs(task): close entity reconciliation phase at blocker'
          git push origin HEAD:docs/OTC2-20260803-canary-entity-reconciliation-closeout

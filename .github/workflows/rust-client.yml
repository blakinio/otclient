name: Rust Client

on:
  pull_request:
    paths:
      - "oteryn-client/**"
      - ".github/workflows/rust-client.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: rust-client-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    name: Rust Client / Windows
    runs-on: windows-latest
    timeout-minutes: 30
    defaults:
      run:
        shell: pwsh
        working-directory: oteryn-client
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
    steps:
      - name: Checkout head for one-time W5 bootstrap
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          show-progress: false

      - name: Install pinned Rust toolchain
        run: |
          $output = rustup toolchain install 1.94.0 --profile minimal --component clippy,rustfmt --target x86_64-pc-windows-msvc 2>&1
          if ($LASTEXITCODE -ne 0) {
            throw ($output -join "`n")
          }
          rustup default 1.94.0 | Out-Null
          $version = rustc --version
          if ($version -notmatch '^rustc 1\.94\.0 ') {
            throw "Unexpected Rust compiler: $version"
          }
          Write-Host $version
          cargo --version

      - name: Commit generated W5 lock and formatting
        run: |
          cargo generate-lockfile
          cargo fmt --all
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Cargo.lock Cargo.toml apps/client/Cargo.toml apps/client/src/main.rs crates/renderer/Cargo.toml crates/renderer/src/lib.rs crates/renderer/src/windows.rs
          git diff --cached --quiet
          if ($LASTEXITCODE -eq 0) {
            Write-Host "No generated W5 changes to commit"
            exit 0
          }
          git commit -m "chore(rust): generate W5 renderer lockfile"
          git push origin "HEAD:${{ github.head_ref }}"

      - name: Validate locked workspace metadata
        run: cargo metadata --locked --format-version 1 | Out-Null

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Lint workspace
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Test workspace
        run: cargo test --workspace --all-targets --locked

      - name: Validate architecture policy
        run: cargo run --locked -p oteryn-architecture-check -- workspace .

  supply-chain:
    name: Rust Client / Supply Chain
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          show-progress: false

      - name: Check advisories licenses bans and sources
        uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25 # cargo-deny 0.20.2
        with:
          rust-version: "1.94.0"
          manifest-path: "oteryn-client/Cargo.toml"
          arguments: "--all-features"
          command: "check"

name: Track A filesystem-helper resolver static probe

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/tibia-official-client-re-filesystem-helper-resolver-static.yml
      - docs/agents/tasks/active/OTC-20260816-track-a-filesystem-helper-resolver-static.md

permissions:
  contents: read

jobs:
  inspect:
    name: Map first BEClient path component statically
    runs-on: [otclient, synology]
    timeout-minutes: 6
    steps:
      - name: Trace TTibiaFileSystemHelper slot plus 0x28
        shell: bash
        run: |
          set -Eeuo pipefail
          set +x
          [[ "${RUNNER_NAME:-}" == 'synology-otclient-01' ]] || exit 2
          client='/home/runner/_work/_otclient_tibia_re_state/tasks/OTC-20260815-track-a-runtime-reacquisition/runs/31904939696/package/bin/client'
          [[ -f "$client" ]] || exit 3
          [[ "$(stat -c '%s' "$client")" == '51965216' ]] || exit 4
          [[ "$(sha256sum "$client" | awk '{print $1}')" == 'e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe' ]] || exit 5
          echo 'FS_RESOLVER_CLIENT_FENCE=PASS'
          python3 - "$client" <<'PY'
          import hashlib,pathlib,struct,sys
          d=pathlib.Path(sys.argv[1]).read_bytes();E='<'
          h=struct.unpack_from(E+'HHIQQQIHHHHHH',d,16)
          _,_,_,_,_,shoff,_,_,_,_,shentsz,shnum,shstrndx=h
          sf=E+'IIQQQQIIQQ';raw=[struct.unpack_from(sf,d,shoff+i*shentsz) for i in range(shnum)]
          st=raw[shstrndx];sn=d[st[4]:st[4]+st[5]]
          def cs(b,o):
              if not (0<=o<len(b)):return ''
              e=b.find(b'\0',o);e=len(b) if e<0 else e
              return b[o:e].decode('utf8','replace')
          secs=[]
          for i,x in enumerate(raw):secs.append({'idx':i,'name':cs(sn,x[0]),'addr':x[3],'off':x[4],'size':x[5],'link':x[6],'entsize':x[9]})
          by={s['name']:s for s in secs};text=by['.text']
          def voff(va):
              for s in secs:
                  if s['size'] and s['addr']<=va<s['addr']+s['size']:return s['off']+(va-s['addr'])
          def b_at(va,n):
              o=voff(va);return b'' if o is None else d[o:o+n]
          def s_at(va):
              o=voff(va)
              if o is None:return None
              e=d.find(b'\0',o,min(len(d),o+240))
              if e<=o:return None
              z=d[o:e]
              if not z or len(z)>220 or not all(32<=x<127 for x in z):return None
              return z.decode('ascii','replace')
          rel=by['.rela.dyn'];bo={}
          for o in range(rel['off'],rel['off']+rel['size'],rel['entsize'] or 24):
              ro,ri,ad=struct.unpack_from('<QQq',d,o);bo[ro]=(ri&0xffffffff,ri>>32,ad)
          rt=lambda va: None if va not in bo else bo[va][2]
          tvptr=0x2f62080;tti=0x3070940
          if rt(tvptr-8)!=tti:raise SystemExit('FS_RESOLVER_FAIL=ttibia_typeinfo')
          if rt(tvptr+0x28)!=0xc9b620:raise SystemExit(f'FS_RESOLVER_FAIL=ttibia_slot28:{rt(tvptr+0x28)}')
          print('FS_RESOLVER_PASS=TTibia_slot28_0xc9b620')
          start,end=0xc9b620,0xc9b890;blob=b_at(start,end-start)
          print(f'FS_RESOLVER_SLOT28_SHA256={hashlib.sha256(blob).hexdigest()}')
          print(f'FS_RESOLVER_SLOT28_HEX={blob.hex()}')
          print('FS_RESOLVER_SLOT28_RIP_STRINGS_BEGIN')
          for i in range(len(blob)-7):
              if blob[i] not in range(0x48,0x50) or blob[i+1] not in (0x8d,0x8b):continue
              m=blob[i+2]
              if (m>>6)!=0 or (m&7)!=5:continue
              va=start+i;dst=va+7+struct.unpack_from('<i',blob,i+3)[0];txt=s_at(dst)
              if txt:print(f'ref=0x{va:x} target=0x{dst:x} text={txt}')
          print('FS_RESOLVER_SLOT28_RIP_STRINGS_END')
          # Exact caller builds two QString elements: slot28 result and key argument at RCX.
          for va,hx,label in [
              (0xc9b8a7,'488b064989e54c89efff5028','call_ttibia_slot28'),
              (0xc9b8b7,'660f6f1424488b542410','copy_slot28_result'),
              (0xc9b8df,'f3410f6f1c24498b542410','copy_key_argument'),
              (0xc9b905,'bf30000000','allocate_two_qstring_elements'),
              (0xc9b96f,'4889de4889ef4c89fa41ffd6','call_base_slot78'),
          ]:
              got=b_at(va,len(bytes.fromhex(hx)))
              if got!=bytes.fromhex(hx):raise SystemExit(f'FS_RESOLVER_FAIL={label}:{got.hex()}')
              print(f'FS_RESOLVER_PASS={label}@0x{va:x}')
          print('FS_RESOLVER_EXECUTED_TARGET=false')
          print('FS_RESOLVER_LIVE_RUNTIME_OBSERVED=false')
          print('FS_RESOLVER_MUTATION_PERFORMED=false')
          PY

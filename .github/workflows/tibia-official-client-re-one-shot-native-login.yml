name: Track A bounded one-shot native login

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/tibia-official-client-re-one-shot-native-login.yml

permissions:
  contents: read

concurrency:
  group: track-a-bounded-one-shot-native-login
  cancel-in-progress: false

env:
  EXPECTED_SHA: ed5469b9fa71349de688f719434d23875f76f28a3ebd08a36d30f7f6da0af6b8
  EXPECTED_SIZE: '52109920'
  CONTAINER: otclient-track-a-kasmvnc
  CLIENT: /home/kasm-user/otclient-track-a/Tibia-32177065988-1/bin/client
  TASK_ROOT: /tmp/otclient-native-login-current-sha
  BRIDGE_SOCKET: /tmp/otclient-native-login-current-sha/bridge.sock
  AUTH_SOCKET: /tmp/otclient-native-login-current-sha/auth.sock
  CHARACTER_SOCKET: /tmp/otclient-native-login-current-sha/character.sock
  CLIENT_VERSION: 15.32.current.ed5469
  TARGETS: player_protocol_handler,30bf620,tibia::game::TPlayerProtocolMessageHandler;gameserver_game_session,30aff80,tibia::game::TGameserverGameSession;worldmap_handler,30c01d0,tibia::worldmap::TWorldmapProtocolMessageHandler

jobs:
  login:
    runs-on: [otclient, synology]
    timeout-minutes: 8
    steps:
      - name: Verify exact isolated runtime and current-SHA helpers
        shell: bash
        run: |
          set +x
          set -Eeuo pipefail
          unset TIBIA_TEST_EMAIL TIBIA_TEST_PASSWORD
          [[ "$RUNNER_NAME" == synology-otclient-01 ]]
          test "$(docker inspect "$CONTAINER" --format '{{.State.Status}}')" = running
          test "$(docker exec -u kasm-user "$CONTAINER" stat -c %s "$CLIENT")" = "$EXPECTED_SIZE"
          test "$(docker exec -u kasm-user "$CONTAINER" sha256sum "$CLIENT" | awk '{print $1}')" = "$EXPECTED_SHA"
          readarray -t pids < <(docker exec -i -u kasm-user -e CLIENT="$CLIENT" "$CONTAINER" python3 - <<'PY'
          import os,pathlib
          expected=os.path.realpath(os.environ['CLIENT'])
          for p in pathlib.Path('/proc').iterdir():
              if not p.name.isdigit(): continue
              try: x=os.path.realpath(str(p/'exe'))
              except OSError: continue
              if x==expected: print(p.name)
          PY
          )
          test "${#pids[@]}" = 1
          for f in bridge.so auth.so character.so secret-ingress; do docker exec -u kasm-user "$CONTAINER" test -s "$TASK_ROOT/$f"; done
          docker exec -u kasm-user "$CONTAINER" strings "$TASK_ROOT/auth.so" > "$RUNNER_TEMP/auth.strings"
          docker exec -u kasm-user "$CONTAINER" strings "$TASK_ROOT/character.so" > "$RUNNER_TEMP/character.strings"
          grep -qx "$EXPECTED_SHA" "$RUNNER_TEMP/auth.strings"
          grep -qx "$EXPECTED_SHA" "$RUNNER_TEMP/character.strings"
          echo ISOLATED_TARGET_UNIQUENESS=PASS
          echo CURRENT_HELPER_SET=PASS
          echo SECRET_ACCESS=false

      - name: Relaunch exact client with helper preload
        shell: bash
        run: |
          set +x
          set -Eeuo pipefail
          unset TIBIA_TEST_EMAIL TIBIA_TEST_PASSWORD
          old_pid="$(docker exec -u kasm-user "$CONTAINER" pgrep -x client)"
          [[ "$old_pid" =~ ^[0-9]+$ ]]
          docker exec -u kasm-user "$CONTAINER" kill -TERM "$old_pid"
          for _ in $(seq 1 80); do docker exec "$CONTAINER" test ! -d "/proc/$old_pid" && break; sleep .1; done
          docker exec "$CONTAINER" test ! -d "/proc/$old_pid"
          docker exec -u kasm-user "$CONTAINER" rm -f "$BRIDGE_SOCKET" "$AUTH_SOCKET" "$CHARACTER_SOCKET"
          client_dir="${CLIENT%/client}"; preload="$TASK_ROOT/bridge.so:$TASK_ROOT/auth.so:$TASK_ROOT/character.so"
          docker exec -d -u kasm-user -w "$client_dir" \
            -e HOME=/home/kasm-user -e DISPLAY=:1 -e XAUTHORITY=/home/kasm-user/.Xauthority \
            -e LD_LIBRARY_PATH="$client_dir:$client_dir/lib" -e LD_PRELOAD="$preload" \
            -e OTCLIENT_TIBIA_RE_SOCKET="$BRIDGE_SOCKET" -e OTCLIENT_TIBIA_RE_AUTH_SOCKET="$AUTH_SOCKET" \
            -e OTCLIENT_TIBIA_RE_CHARACTER_SOCKET="$CHARACTER_SOCKET" -e OTCLIENT_TIBIA_RE_CLIENT_VERSION="$CLIENT_VERSION" \
            -e OTCLIENT_TIBIA_RE_BINARY_SHA256="$EXPECTED_SHA" -e OTCLIENT_TIBIA_RE_TARGETS="$TARGETS" \
            "$CONTAINER" ./client
          for _ in $(seq 1 120); do
            ok=true; for s in "$BRIDGE_SOCKET" "$AUTH_SOCKET" "$CHARACTER_SOCKET"; do docker exec -u kasm-user "$CONTAINER" test -S "$s" || ok=false; done
            $ok && break; sleep .25
          done
          for s in "$BRIDGE_SOCKET" "$AUTH_SOCKET" "$CHARACTER_SOCKET"; do docker exec -u kasm-user "$CONTAINER" test -S "$s"; done
          runtime_pid="$(docker exec -u kasm-user "$CONTAINER" pgrep -x client)"; [[ "$runtime_pid" =~ ^[0-9]+$ ]]
          test "$(docker exec -u kasm-user "$CONTAINER" stat -c %s /proc/$runtime_pid/exe)" = "$EXPECTED_SIZE"
          test "$(docker exec -u kasm-user "$CONTAINER" sha256sum /proc/$runtime_pid/exe | awk '{print $1}')" = "$EXPECTED_SHA"
          docker exec -u kasm-user "$CONTAINER" sh -lc "! tr '\\0' '\\n' </proc/$runtime_pid/environ | grep -Eq '^TIBIA_TEST_(EMAIL|PASSWORD)='"
          printf '%s\n' "$runtime_pid" > "$RUNNER_TEMP/native-login-pid"
          echo HELPER_RUNTIME_RELAUNCH=PASS
          echo PERSISTENT_SECRET_ENVIRONMENT=false

      - name: Bounded one-shot native authentication
        shell: bash
        env:
          TIBIA_TEST_EMAIL: ${{ secrets.TIBIA_TEST_EMAIL }}
          TIBIA_TEST_PASSWORD: ${{ secrets.TIBIA_TEST_PASSWORD }}
        run: |
          set +x
          set -Eeuo pipefail
          test -n "${TIBIA_TEST_EMAIL:-}"; test -n "${TIBIA_TEST_PASSWORD:-}"
          runtime_pid="$(cat "$RUNNER_TEMP/native-login-pid")"
          set +e
          docker exec -i -u kasm-user -e TIBIA_TEST_EMAIL -e TIBIA_TEST_PASSWORD "$CONTAINER" \
            "$TASK_ROOT/secret-ingress" "$AUTH_SOCKET" "$runtime_pid" >"$RUNNER_TEMP/auth.out" 2>&1
          rc=$?
          set -e
          unset TIBIA_TEST_EMAIL TIBIA_TEST_PASSWORD
          if [[ $rc -eq 0 ]] && grep -Fq 'NATIVE_AUTH_DISPATCH=PASS' "$RUNNER_TEMP/auth.out"; then
            echo NATIVE_AUTH_INGRESS=PASS_RESPONSE
          elif grep -Fq 'NATIVE_AUTH_RESPONSE_FAILED' "$RUNNER_TEMP/auth.out"; then
            echo NATIVE_AUTH_INGRESS=PASS_WITH_PROCESS_HANDOFF
          else
            echo NATIVE_AUTH_INGRESS=FAIL
            sed -n 's/^current secret ingress error: /INGRESS_ERROR=/p' "$RUNNER_TEMP/auth.out"
            exit 2
          fi
          echo SECRET_VALUES_LOGGED=false
          echo SECOND_SECRET_ATTEMPT=false

      - name: Restore helper instrumentation after native handoff without credentials
        shell: bash
        run: |
          set +x
          set -Eeuo pipefail
          unset TIBIA_TEST_EMAIL TIBIA_TEST_PASSWORD
          sleep 2
          readarray -t pids < <(docker exec -i -u kasm-user -e CLIENT="$CLIENT" "$CONTAINER" python3 - <<'PY'
          import os,pathlib
          expected=os.path.realpath(os.environ['CLIENT'])
          for p in pathlib.Path('/proc').iterdir():
              if not p.name.isdigit(): continue
              try: x=os.path.realpath(str(p/'exe'))
              except OSError: continue
              if x==expected: print(p.name)
          PY
          )
          test "${#pids[@]}" = 1
          pid="${pids[0]}"
          need_restore=true
          if docker exec -u kasm-user "$CONTAINER" test -S "$BRIDGE_SOCKET" && \
             docker exec -u kasm-user "$CONTAINER" sh -lc "tr '\\0' '\\n' </proc/$pid/environ | grep -Fxq 'LD_PRELOAD=$TASK_ROOT/bridge.so:$TASK_ROOT/auth.so:$TASK_ROOT/character.so'"; then
            need_restore=false
          fi
          if $need_restore; then
            docker exec -u kasm-user "$CONTAINER" kill -TERM "$pid"
            for _ in $(seq 1 80); do docker exec "$CONTAINER" test ! -d "/proc/$pid" && break; sleep .1; done
            docker exec "$CONTAINER" test ! -d "/proc/$pid"
            docker exec -u kasm-user "$CONTAINER" rm -f "$BRIDGE_SOCKET" "$AUTH_SOCKET" "$CHARACTER_SOCKET"
            client_dir="${CLIENT%/client}"; preload="$TASK_ROOT/bridge.so:$TASK_ROOT/auth.so:$TASK_ROOT/character.so"
            docker exec -d -u kasm-user -w "$client_dir" \
              -e HOME=/home/kasm-user -e DISPLAY=:1 -e XAUTHORITY=/home/kasm-user/.Xauthority \
              -e LD_LIBRARY_PATH="$client_dir:$client_dir/lib" -e LD_PRELOAD="$preload" \
              -e OTCLIENT_TIBIA_RE_SOCKET="$BRIDGE_SOCKET" -e OTCLIENT_TIBIA_RE_AUTH_SOCKET="$AUTH_SOCKET" \
              -e OTCLIENT_TIBIA_RE_CHARACTER_SOCKET="$CHARACTER_SOCKET" -e OTCLIENT_TIBIA_RE_CLIENT_VERSION="$CLIENT_VERSION" \
              -e OTCLIENT_TIBIA_RE_BINARY_SHA256="$EXPECTED_SHA" -e OTCLIENT_TIBIA_RE_TARGETS="$TARGETS" \
              "$CONTAINER" ./client
          fi
          for _ in $(seq 1 120); do docker exec -u kasm-user "$CONTAINER" test -S "$BRIDGE_SOCKET" && docker exec -u kasm-user "$CONTAINER" test -S "$CHARACTER_SOCKET" && break; sleep .25; done
          docker exec -u kasm-user "$CONTAINER" test -S "$BRIDGE_SOCKET"; docker exec -u kasm-user "$CONTAINER" test -S "$CHARACTER_SOCKET"
          restored_pid="$(docker exec -u kasm-user "$CONTAINER" pgrep -x client)"; [[ "$restored_pid" =~ ^[0-9]+$ ]]
          docker exec -u kasm-user "$CONTAINER" sh -lc "! tr '\\0' '\\n' </proc/$restored_pid/environ | grep -Eq '^TIBIA_TEST_(EMAIL|PASSWORD)='"
          printf '%s\n' "$restored_pid" > "$RUNNER_TEMP/native-login-pid"
          echo POST_AUTH_HELPER_RESTORE=PASS
          echo SECOND_SECRET_ATTEMPT=false

      - name: Confirm singleton character only if needed
        shell: bash
        run: |
          set +x
          set -Eeuo pipefail
          runtime_pid="$(cat "$RUNNER_TEMP/native-login-pid")"
          docker exec -i -u kasm-user -e BSOCK="$BRIDGE_SOCKET" -e CSOCK="$CHARACTER_SOCKET" -e EXPECTED_PID="$runtime_pid" "$CONTAINER" python3 - <<'PY'
          import json,os,socket,struct,time
          expected=int(os.environ['EXPECTED_PID'])
          def call(path,cmd):
              s=socket.socket(socket.AF_UNIX,socket.SOCK_STREAM); s.settimeout(5); s.connect(path)
              pid,uid,gid=struct.unpack('3i',s.getsockopt(socket.SOL_SOCKET,socket.SO_PEERCRED,12)); assert pid==expected and uid==os.geteuid()
              s.sendall(cmd.encode()+b'\n'); raw=b''
              while b'\n' not in raw: raw+=s.recv(4096)
              s.close(); return json.loads(raw.split(b'\n',1)[0])
          names=('player_protocol_handler','gameserver_game_session','worldmap_handler')
          if all(call(os.environ['BSOCK'],'DISCOVER '+n).get('validated_hits')==1 for n in names):
              print('CHARACTER_CONFIRM=NOT_NEEDED_ALREADY_IN_GAME'); raise SystemExit(0)
          state={}
          for _ in range(120):
              try: state=call(os.environ['CSOCK'],'STATE')
              except (OSError,AssertionError): state={}
              count=state.get('character_count',-1) if state.get('ok') else -1
              if isinstance(count,int) and count>0: break
              time.sleep(.5)
          count=state.get('character_count',-1); print('NATIVE_CHARACTER_COUNT='+str(count))
          if count!=1: raise SystemExit(78)
          r=call(os.environ['CSOCK'],'CONFIRM_UNIQUE'); assert r.get('ok') and r.get('confirmation_dispatched') and r.get('character_index')==0
          print('NATIVE_CHARACTER_CONFIRM=PASS')
          PY

      - name: Prove structural IN_GAME
        shell: bash
        run: |
          set +x
          set -Eeuo pipefail
          runtime_pid="$(cat "$RUNNER_TEMP/native-login-pid")"
          docker exec -i -u kasm-user -e SOCKET="$BRIDGE_SOCKET" -e EXPECTED_PID="$runtime_pid" "$CONTAINER" python3 - <<'PY'
          import json,os,socket,struct,time
          path=os.environ['SOCKET']; expected=int(os.environ['EXPECTED_PID'])
          def call(cmd):
              s=socket.socket(socket.AF_UNIX,socket.SOCK_STREAM); s.settimeout(5); s.connect(path)
              pid,uid,gid=struct.unpack('3i',s.getsockopt(socket.SOL_SOCKET,socket.SO_PEERCRED,12)); assert pid==expected and uid==os.geteuid()
              s.sendall(cmd.encode()+b'\n'); raw=b''
              while b'\n' not in raw: raw+=s.recv(4096)
              s.close(); return json.loads(raw.split(b'\n',1)[0])
          names=('player_protocol_handler','gameserver_game_session','worldmap_handler'); last={}
          for _ in range(120):
              try: last={n:call('DISCOVER '+n) for n in names}
              except (OSError,AssertionError): last={}; time.sleep(1); continue
              if all(d.get('ok') and d.get('validated_hits')==1 for d in last.values()):
                  print('RESULT=SUCCESS'); print('CHARACTER_ACTUALLY_LOGGED_INTO_GAME=YES'); print('CAUSAL_PROOF=COMPLETE'); print('STRUCTURAL_IN_GAME=PASS'); print('SECOND_SECRET_ATTEMPT=false'); raise SystemExit(0)
              time.sleep(1)
          print('STRUCTURAL_IN_GAME=FAIL')
          for n in names: print(n.upper()+'_HITS='+str(last.get(n,{}).get('validated_hits','UNKNOWN')))
          raise SystemExit(2)
          PY
name: Track A live tunneled session sockets

on:
  push:
    branches: [ci/OTC-20260813-official-client-re-continuation]
    paths:
      - .github/workflows/tibia-official-client-re-session-sockets.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: [otclient, synology]
    timeout-minutes: 2
    env:
      CANONICAL_STATE: /home/runner/_work/_otclient_tibia_re_state
      LEGACY_IMAGE_STATE: /work/_otclient_tibia_re_state
    steps:
      - name: Read detached initial-map capture
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$GITHUB_REPOSITORY" == blakinio/otclient && "$RUNNER_NAME" == synology-otclient-01 ]]
          if [[ -d /work && -w /work ]]; then state="$LEGACY_IMAGE_STATE"; else state="$CANONICAL_STATE"; fi
          watchdir="$state/evidence/initial-map-detached"
          echo TRACK_A_WATCHER_LOG_BEGIN
          cat "$watchdir/watcher.log" 2>/dev/null || true
          echo TRACK_A_WATCHER_LOG_END
          echo TRACK_A_WATCHER_RESULT_BEGIN
          cat "$watchdir/result.txt" 2>/dev/null || true
          echo TRACK_A_WATCHER_RESULT_END
          records="$watchdir/records.txt"
          if [[ -f "$records" ]]; then
            echo TRACK_A_WATCHER_RECORD_LINES="$(wc -l <"$records")"
            awk '{zs[$4]=1} END {printf "TRACK_A_WATCHER_LIVE_Z_VALUES="; first=1; for(z in zs){if(!first)printf ","; printf "%s",z; first=0} printf "\n"}' "$records"
            echo TRACK_A_WATCHER_SAMPLE_BEGIN
            head -20 "$records"
            echo TRACK_A_WATCHER_SAMPLE_END
          fi

name: Track A isolated default Xvfb DRI path probe

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/tibia-official-client-re-xvfb-dri-path-default-glx.yml

permissions:
  contents: read

concurrency:
  group: track-a-xvfb-dri-path-default-glx
  cancel-in-progress: false

jobs:
  xvfb-dri-path-default-glx:
    runs-on: [otclient, synology]
    timeout-minutes: 6
    steps:
      - name: Start default isolated Xvfb with contained DRI provider path
        shell: bash
        env:
          EXPECTED_BASE_SHA: d3f186414256151c9d5e03f34c5a9026b1fba500
          EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set +x
          set -Eeuo pipefail
          umask 077
          [[ "$RUNNER_NAME" == synology-otclient-01 ]]
          [[ "$GITHUB_REPOSITORY" == blakinio/otclient ]]
          [[ "$EVENT_BASE_SHA" == "$EXPECTED_BASE_SHA" ]] || exit 44

          task='OTC-20260816-track-a-xvfb-dri-path-default-glx'
          marker="OTCLIENT_TIBIA_RE_DIAG_TASK=$task"
          role='xvfb-dri-path-default-glx'
          tool='/work/_otclient_tibia_re_state/toolroot'
          xvfb="$tool/usr/bin/Xvfb"
          dri="$tool/usr/lib/x86_64-linux-gnu/dri"
          swrast="$dri/swrast_dri.so"
          xkb="$tool/usr/share/X11/xkb"
          xkbcomp='/usr/bin/xkbcomp'
          xvfb_sha='2c7f5a9534410fed5092d782a69ca7ffd9fce80e98b81ffe4944d703dd11d3b1'
          swrast_sha='c28638b02783ebc96a78bb982fe59ad0d54230bc1faf53305af33edab29cd388'
          xkbcomp_sha='0967e7e7b03b077327cea74567726b265bd304b4fdf59f87bf7fdfe1074e7591'
          root="/home/runner/_work/_otclient_tibia_re_state/tasks/$task/ephemeral-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          pid=''
          display_number=''

          task_owned_pid() {
            local p="$1"
            [[ "$p" =~ ^[1-9][0-9]*$ && -r "/proc/$p/environ" ]] || return 1
            grep -azFxq "$marker" "/proc/$p/environ" || return 1
            grep -azFxq "OTCLIENT_TIBIA_RE_ROLE=$role" "/proc/$p/environ"
          }

          cleanup() {
            rc=$?
            if [[ -n "$pid" ]] && task_owned_pid "$pid"; then
              kill "$pid" 2>/dev/null || true
              for _ in $(seq 1 20); do kill -0 "$pid" 2>/dev/null || break; sleep .1; done
              kill -9 "$pid" 2>/dev/null || true
              wait "$pid" 2>/dev/null || true
            fi
            if [[ -n "$display_number" && -f "/tmp/.X${display_number}-lock" ]]; then
              owner="$(tr -cd '0-9' <"/tmp/.X${display_number}-lock" 2>/dev/null || true)"
              if [[ "$owner" == "$pid" && ! -e "/proc/$pid" ]]; then
                rm -f -- "/tmp/.X${display_number}-lock" "/tmp/.X11-unix/X${display_number}"
              fi
            fi
            rm -rf --one-file-system "$root" 2>/dev/null || true
            echo 'XVFB_DRI_DEFAULT_CLEANUP=COMPLETE'
            exit "$rc"
          }
          trap cleanup EXIT

          echo "XVFB_DRI_DEFAULT_TASK=$task"
          echo 'XVFB_DRI_DEFAULT_ADMISSION=EPHEMERAL_ISOLATED_MUTATION_AUTHORIZED'
          echo 'XVFB_DRI_DEFAULT_CANONICAL_STATE_ACCESS=NONE'
          echo 'XVFB_DRI_DEFAULT_CLIENT_STARTED=false'
          echo 'XVFB_DRI_DEFAULT_VNC_STARTED=false'
          echo 'XVFB_DRI_DEFAULT_WARP_STARTED=false'
          echo 'XVFB_DRI_DEFAULT_EXPLICIT_GLX_FLAG=false'

          [[ -d "$tool" && ! -L "$tool" && "$(realpath -e "$tool")" == "$tool" ]] || exit 45
          [[ -f "$xvfb" && -x "$xvfb" && ! -L "$xvfb" ]] || exit 46
          [[ "$(sha256sum "$xvfb" | awk '{print $1}')" == "$xvfb_sha" ]] || exit 47
          [[ -d "$dri" && ! -L "$dri" && "$(realpath -e "$dri")" == "$dri" ]] || exit 48
          [[ -e "$swrast" ]] || exit 49
          swrast_real="$(realpath -e "$swrast")"
          [[ "$swrast_real" == "$dri"/* ]] || exit 50
          [[ "$(sha256sum "$swrast" | awk '{print $1}')" == "$swrast_sha" ]] || exit 51
          [[ -d "$xkb" && ! -L "$xkb" ]] || exit 52
          [[ -f "$xkbcomp" && -x "$xkbcomp" && ! -L "$xkbcomp" ]] || exit 53
          [[ "$(sha256sum "$xkbcomp" | awk '{print $1}')" == "$xkbcomp_sha" ]] || exit 54
          echo 'XVFB_DRI_DEFAULT_SUPPORT_FENCE=PASS'
          echo "XVFB_DRI_DEFAULT_SWRAST_REAL=$swrast_real"
          echo "XVFB_DRI_DEFAULT_PATH=$dri"

          for n in $(seq 244 250); do
            if [[ ! -e "/tmp/.X${n}-lock" && ! -e "/tmp/.X11-unix/X${n}" ]]; then display_number="$n"; break; fi
          done
          [[ -n "$display_number" ]] || exit 55
          display=":$display_number"
          mkdir -p "$root/home"
          chmod 700 "$root" "$root/home"
          log="$root/xvfb.log"
          echo "XVFB_DRI_DEFAULT_DISPLAY=$display"

          env -u RUNNER_TRACKING_ID -u TIBIA_TEST_EMAIL -u TIBIA_TEST_PASSWORD \
            -u LIBGL_ALWAYS_SOFTWARE -u GALLIUM_DRIVER -u MESA_LOADER_DRIVER_OVERRIDE \
            OTCLIENT_TIBIA_RE_DIAG_TASK="$task" OTCLIENT_TIBIA_RE_ROLE="$role" \
            HOME="$root/home" PATH="$tool/usr/bin:$tool/usr/sbin:/usr/bin:/bin" \
            LD_LIBRARY_PATH="$tool/usr/lib/x86_64-linux-gnu:$tool/lib/x86_64-linux-gnu" \
            LIBGL_DRIVERS_PATH="$dri" XKB_CONFIG_ROOT="$xkb" \
            nohup "$xvfb" "$display" -screen 0 1920x1080x24 -xkbdir "$xkb" \
            -nolisten tcp -noreset >"$log" 2>&1 </dev/null &
          pid=$!
          echo "XVFB_DRI_DEFAULT_PID=$pid"

          for _ in $(seq 1 60); do
            [[ -e "/tmp/.X11-unix/X${display_number}" ]] && break
            kill -0 "$pid" 2>/dev/null || break
            sleep .2
          done
          if [[ ! -e "/tmp/.X11-unix/X${display_number}" ]]; then
            echo 'XVFB_DRI_DEFAULT_SERVER_STARTED=false'
          else
            task_owned_pid "$pid" || exit 56
            echo 'XVFB_DRI_DEFAULT_SERVER_STARTED=true'
            python3 - "$display_number" <<'PY'
          import socket,struct,sys
          n=int(sys.argv[1]); s=socket.socket(socket.AF_UNIX,socket.SOCK_STREAM); s.settimeout(3); s.connect(f'/tmp/.X11-unix/X{n}')
          def rx(k):
              b=b''
              while len(b)<k:
                  c=s.recv(k-len(b))
                  if not c: raise SystemExit('unexpected_eof')
                  b+=c
              return b
          s.sendall(struct.pack('<BBHHHHH',ord('l'),0,11,0,0,0,0)); h=rx(8); rx(struct.unpack_from('<H',h,6)[0]*4)
          s.sendall(struct.pack('<BBH',99,0,1)); r=rx(32); payload=rx(struct.unpack_from('<I',r,4)[0]*4)
          names=[]; p=0
          for _ in range(r[1]): ln=payload[p]; p+=1; names.append(payload[p:p+ln].decode('ascii','replace')); p+=ln
          def q(name):
              raw=name.encode(); pad=(-len(raw))%4; length=(8+len(raw)+pad)//4
              s.sendall(struct.pack('<BBHHH',98,0,length,len(raw),0)+raw+b'\0'*pad); rep=rx(32); return bool(rep[8]),int(rep[9])
          glx,go=q('GLX'); render,ro=q('RENDER')
          print(f'XVFB_DRI_DEFAULT_EXTENSION_COUNT={len(names)}')
          print('XVFB_DRI_DEFAULT_EXTENSIONS='+','.join(sorted(names)))
          print(f'XVFB_DRI_DEFAULT_GLX_PRESENT={str(glx).lower()}')
          print(f'XVFB_DRI_DEFAULT_GLX_MAJOR_OPCODE={go if glx else 0}')
          print(f'XVFB_DRI_DEFAULT_RENDER_PRESENT={str(render).lower()}')
          print(f'XVFB_DRI_DEFAULT_RENDER_MAJOR_OPCODE={ro if render else 0}')
          s.close()
          PY
          fi

          sleep .5
          python3 - "$log" <<'PY'
          import pathlib,re,sys
          p=pathlib.Path(sys.argv[1]); text=p.read_text(errors='replace') if p.exists() else ''
          chosen=[]
          for line in text.splitlines():
              if re.search(r'(?i)(GLX|AIGLX|swrast|renderer|dlopen|driver|provider|DRI|error|failed)',line):
                  chosen.append(''.join(ch if ch.isprintable() else '?' for ch in line)[:400])
          print(f'XVFB_DRI_DEFAULT_LOG_TOTAL_LINES={len(text.splitlines())}')
          print(f'XVFB_DRI_DEFAULT_LOG_FILTER_MATCHES={len(chosen)}')
          for line in chosen[:120]: print('XVFB_DRI_DEFAULT_LOG='+line)
          PY

          echo 'XVFB_DRI_DEFAULT_RESULT=PASS_DISCRIMINATOR_CAPTURED'

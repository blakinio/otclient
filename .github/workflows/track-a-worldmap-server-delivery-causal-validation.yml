name: Track A worldmap server-delivery causal validation

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/track-a-worldmap-server-delivery-causal-validation.yml

permissions:
  contents: read

concurrency:
  group: track-a-worldmap-server-delivery-causal-validation-no-client-hold-v3
  cancel-in-progress: false

jobs:
  no-client-hold:
    name: No-client hold after v3 field discriminator
    runs-on: ubuntu-latest
    steps:
      - name: Publish fail-closed hold
        shell: bash
        run: |
          set -Eeuo pipefail
          echo 'WORLDMAP_PRESECRET_HOLD=ACTIVE'
          echo 'WORLDMAP_PRESECRET_CAUSAL_DISCRIMINATOR=v3_two_local_clusters_upper_unmasked_lower_editable_unclassified'
          echo 'WORLDMAP_PRESECRET_CLIENT_EXECUTED=false'
          echo 'WORLDMAP_PRESECRET_SECRET_USED=false'
          echo 'WORLDMAP_PRESECRET_LOGIN_SUBMITTED=false'
          echo 'WORLDMAP_PRESECRET_LOGIN_BUDGET_CONSUMED=0'

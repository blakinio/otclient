name: W7 Login Finalize

on:
  push:
    branches:
      - feat/OTC2-20260731-w7-login-e2e-final

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout complete branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: feat/OTC2-20260731-w7-login-e2e-final
          fetch-depth: 0
          show-progress: false

      - name: Restack and write final evidence
        run: |
          git fetch origin main
          git merge --no-edit origin/main
          python - <<'PY'
          from pathlib import Path

          def replace(path: str, old: str, new: str) -> None:
              target = Path(path)
              text = target.read_text(encoding='utf-8')
              if old not in text:
                  raise SystemExit(f'expected text not found in {path}: {old[:80]!r}')
              target.write_text(text.replace(old, new, 1), encoding='utf-8')

          Path('oteryn-client/docs/research/technical-login/W7_LOGIN_E2E_EVIDENCE.md').write_text('''# W7 Technical Login Application Integration Evidence

Status: repository/fake technical flow complete; external real-path evidence blocked  
Lane: `W7-LOGIN-E2E`  
Branch: `feat/OTC2-20260731-w7-login-e2e-final`  
PR: #118

## Exact merged producers

- W7 entry contract: `9ecc43a4465f6565bc1c12ea61f170a96edcbe35`;
- W7 Identity/Platform: `d66da47a33d6639876f3edda2b2c08709d1b7a5e`;
- W7 Canary entry: `4a193bdf10ac32a8a2d8dc12f31706c7d668c8f9`;
- Canary lifecycle archive and launch base: `065a37bcc1eab752f2a5504ba58882c4c9a6e6c9`;
- exact final restack main: `a43cf375dc25b95df790e515ca222bc80cae26e1`.

## Delivered composition

`oteryn-app-runtime` owns only top-level orchestration. It allocates one non-zero attempt generation, runs Identity and admission in separately owned cancellable threads, accepts only producer-owned account/directory/credential values, validates the explicit selection, moves the lifecycle into the admission worker, joins every worker and restores the producer-owned lifecycle before exposing typed state.

`apps/client` preserves the existing `winit` window, renderer ownership and close ordering. Technical login is disabled unless `OTERYN_TECHNICAL_LOGIN=1` and every required endpoint, public OAuth client ID, world ID/host/port, character ID and timeout is explicitly supplied. No endpoint, password, client secret or credential has a production default. Identity/admission completions enter the event loop as generation-tagged user events; bounded `about_to_wait` polling handles completion races without performing browser, listener, HTTP or TCP work on the event-loop thread.

Production `CanaryEntryAdapter` intentionally remains fail-closed before network and credential handoff because exact approved RSA/transcript/deployment evidence is absent. The application reports the resulting typed recoverable failure and clears the attempt instead of claiming compatibility.

## Automated fake-service evidence

The workspace tests use only original synthetic values and the exact merged producer APIs.

| Scenario | Evidence |
|---|---|
| dynamic callback | fake binder assigns port `49152`; authorization URL and token request use that exact redirect URI |
| strict Identity/Gateway bootstrap | merged `IdentityClient` executes authorization-code exchange, one Game Login Ticket request and one Gateway v1 request against bounded fake HTTP |
| authoritative selection | merged directory validates character `17` belongs to world `9`; focused runtime test rejects an unknown character with `ChooseAnotherCharacter` |
| one credential handoff | merged lifecycle exposes the synthetic credential once and rejects the second handoff as `CredentialAlreadyConsumed` |
| ordered admission | private original marker sequence reaches `0x17 -> 0x1A -> 0xEF -> 0x0A -> 0x0F` before producer-owned `SessionEntered` |
| fresh second attempt | runtime allocates attempt 2 only after attempt 1 is closed and accepts a distinct synthetic credential |
| stale/duplicate callback | merged `AuthorizationTransaction` rejects duplicate completion and mismatched account generation |
| cancellation/close | runtime cancels and joins an active worker, records `SafeCancellation`, then clears through `Closing -> LoggedOut` |
| production fail-closed | merged Canary adapter returns `RealAdmissionUnavailable` while lifecycle remains `CredentialReady`; no network or credential handoff occurs |
| redaction | runtime, worker event, HTTP request and adapter formatting contain no code, access token, ticket or session credential |

Merged Identity security tests additionally cover wrong path/peer/state, malformed/oversized callback, timeout, strict unknown/trailing/oversized Gateway responses, invalid identifiers/ports and world-character relationship failures. Merged Canary tests additionally cover bounded transport/parser errors, denial classifications, timeout, cancellation, abrupt close, expired/consumed/replayed credential and secret-free diagnostics. LOGIN-E2E consumes those exact contracts rather than duplicating public parsers or protocol types.

## Validation

Exact feature head before final documentation/restack: `87ac50efc97b569712276666a30c8d671cc099b1`.

- Rust Client run `30647012485`: success;
  - locked metadata: PASS;
  - `cargo fmt --all --check`: PASS;
  - strict workspace Clippy with `-D warnings`: PASS;
  - all workspace tests/doctests: PASS;
  - architecture policy: PASS;
  - cargo-deny supply chain: PASS.
- repository CI run `30647013322`: success.
- committed `Cargo.lock` retains the pre-existing external resolution, including `xcursor 0.3.10`; only local workspace package entries/dependency edges were generated.

Final exact-head validation is repeated after this documentation/restack commit and before merge.

## Explicit external blockers

### `W7-BLOCK-REAL-RUST-E2E`

No deployed exact Identity, Gateway and Canary revisions, configured public client ID/issuer mapping or fresh controlled credential were available. No real browser-return or Rust admission through `0x0F` was run, and no real compatibility claim is made.

### `W7-BLOCK-DEPLOYMENT-EVIDENCE`

Repository access does not prove deployed TLS, DNS, firewall, hostname, secret-manager or runtime revision state. No production-readiness claim is made.

### Interactive Windows observation

Required Windows compilation is proven by CI, and the existing event-loop/renderer ownership is preserved by source and tests. A named interactive desktop observation of the window remaining responsive during configured login was unavailable in the worker environment and is not claimed.

## Exclusions

No map-description decoding, gameplay state, inventory, chat, combat, reconnect/resume, channel switching, general native UI framework, credential persistence, production assets, updater/deployment code, password fallback, private capture or external-repository write is included.
''', encoding='utf-8')

          replace(
              'docs/agents/MODULE_CATALOG.md',
              '| W7 Canary Current entry transport | active PR #113 |',
              '| W7 Canary Current entry transport | merged PR #113; archived by #116 |',
          )
          replace(
              'docs/agents/MODULE_CATALOG.md',
              '| W7 Platform native-auth boundary | active PR #110 |',
              '| W7 Platform native-auth boundary | merged PR #110; archived by #111 |',
          )
          replace(
              'docs/agents/MODULE_CATALOG.md',
              '| W7 Identity PKCE transaction | active PR #110 |',
              '| W7 Identity PKCE transaction | merged PR #110; archived by #111 |',
          )
          replace(
              'docs/agents/MODULE_CATALOG.md',
              '| W7 Identity security tests | active PR #110 |',
              '| W7 Identity security tests | merged PR #110; archived by #111 |',
          )
          marker = '| Oteryn diagnostics contracts | merged PR #61; archived by #62 |'
          addition = '| W7 technical-login application runtime | active PR #118 | `oteryn-app-runtime` owns generation-checked cancellable/joined Identity and admission workers, bounded phase history and producer-owned `SessionEntered`/`EntryFailure`; the existing Windows shell adds an explicit environment-only technical configuration and typed event-loop integration | `oteryn-client/crates/app-runtime/**`, `oteryn-client/tests/integration/technical-login/**`, `oteryn-client/apps/client/**`, `oteryn-client/docs/research/technical-login/W7_LOGIN_E2E_EVIDENCE.md` | Consumes exact ENTRY/Identity/Canary merges without substitute public contracts. Fake services prove the complete bounded flow and cleanup. Production Canary remains fail-closed; real/deployment and interactive desktop evidence remain explicit blockers without compatibility claims. |\n'
          replace('docs/agents/MODULE_CATALOG.md', marker, addition + marker)

          matrix_marker = '| Launcher/updater | signature/hash/tamper/interruption/rollback tests | Windows clean install/update/rollback evidence |'
          matrix_row = '| W7 technical-login application composition | generation-tagged worker completion, one Identity and one admission worker, explicit cancellation/join, dynamic callback and strict fake Gateway flow, authoritative world-character/route validation, one-shot/duplicate handoff, fresh second attempt, ordered synthetic admission through `0x0F`, shutdown cleanup, redaction and production Canary fail-closed evidence | Windows locked metadata/fmt/strict Clippy/all-target tests/doctests, architecture and cargo-deny. Exact ENTRY/Identity/Canary merge commits and explicit configuration surface must be recorded. Real deployed path and interactive desktop observation remain external blockers and cannot be inferred from fake tests. |\n'
          replace('docs/agents/BUILD_TEST_MATRIX.md', matrix_marker, matrix_row + matrix_marker)
          invariant = '- W7 application composition keeps browser/listener/HTTP/TCP work in explicitly cancelled and joined workers, rejects stale attempt signals, validates configured world ID/host/port and character against the authoritative directory, exposes no credential to the event loop and contains no hidden production defaults;\n'
          replace(
              'docs/agents/BUILD_TEST_MATRIX.md',
              '- W7 Canary production admission remains fail-closed before network and credential handoff until exact approved Current wire and controlled E2E evidence exist; synthetic success is lifecycle/parser evidence only;\n',
              '- W7 Canary production admission remains fail-closed before network and credential handoff until exact approved Current wire and controlled E2E evidence exist; synthetic success is lifecycle/parser evidence only;\n' + invariant,
          )

          replace(
              'docs/agents/CHANGELOG.md',
              '## Unreleased\n\n',
              '## Unreleased\n\n- The greenfield Rust workspace now includes the W7 technical-login application composition: `oteryn-app-runtime` owns generation-tagged cancellable/joined Identity and admission workers and consumes only the merged ENTRY lifecycle; the existing Windows `winit` shell exposes one explicit environment-only technical configuration and handles typed completion events without network work on the event-loop thread. Original fake services prove dynamic callback, strict Gateway bootstrap, authoritative selection, one credential handoff, ordered synthetic admission through `0x0F`, fresh retry, cancellation/close and redaction. Production Canary remains fail-closed, and real deployment plus interactive desktop compatibility remain explicitly unproven.\n',
          )

          replace(
              'oteryn-client/docs/architecture/REPOSITORY_LAYOUT.md',
              '| `app-runtime` | top-level state machine, service composition, error routing | packet parsing, concrete widgets |',
              '| `app-runtime` | generation-checked top-level entry state, owned cancellable/joined worker composition, typed progress/result routing and terminal cleanup | packet parsing, concrete widgets, raw sockets, producer DTOs or credentials outside the one-shot lifecycle |',
          )
          replace(
              'oteryn-client/docs/architecture/REPOSITORY_LAYOUT.md',
              '`apps/client` is the concrete `app` package. Its current W5 boundary composes deterministic shell state with the single `oteryn-renderer` surface owner on the main thread. Protocol, game/domain rendering, feature composition, assets, UI and persistence remain absent.',
              '`apps/client` is the concrete `app` package. It preserves deterministic shell state and the single `oteryn-renderer` surface owner on the main thread, and now composes the W7 technical-login runtime through typed `winit` user events. Browser, callback, HTTP and admission execute only in application-owned workers; explicit environment configuration is opt-in and contains no credentials or production defaults. Game/domain rendering, feature UI, assets and persistence remain absent.',
          )
          paragraph = '\nThe W7 application runtime consumes the exact merged Identity and Canary interfaces plus producer-owned ENTRY lifecycle. It owns no replacement transport/protocol/session contract. The private integration-test package composes original fake browser/listener/HTTP/admission services and cannot establish real Canary or deployment compatibility.\n'
          replace(
              'oteryn-client/docs/architecture/REPOSITORY_LAYOUT.md',
              '\n## 4. Feature crate contract\n',
              paragraph + '\n## 4. Feature crate contract\n',
          )

          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              'The workspace contains seventeen bounded members:',
              'The workspace contains nineteen bounded members:',
          )
          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              'apps/client\ncrates/account-session\n',
              'apps/client\ncrates/account-session\ncrates/app-runtime\n',
          )
          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              'crates/world-directory\ntests/security/auth\n',
              'crates/world-directory\ntests/integration/technical-login\ntests/security/auth\n',
          )
          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              '`oteryn-client` is the bounded Windows application shell and composes one main-thread renderer surface owner.',
              '`oteryn-client` is the bounded Windows application shell and composes one main-thread renderer surface owner plus the explicit W7 technical-login controller. `oteryn-app-runtime` owns generation-checked cancellable/joined Identity and admission workers, typed entry progress/result state and terminal cleanup. `oteryn-technical-login-integration-tests` is an original synthetic-only `tool` package composing the exact merged producer APIs.',
          )
          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              '`tool` packages may consume reviewed lower contracts for development/test/build purposes but must not become runtime service locators or bypass product dependency direction. `oteryn-test-support` depends only on `foundation` and `diagnostics`; `oteryn-asset-compiler` depends only on `oteryn-asset-types` plus the exact workspace JSON parser; `oteryn-identity-security-tests` consumes only the W7 Identity/Platform and merged ENTRY contracts required for synthetic fake-service evidence.',
              '`tool` packages may consume reviewed lower contracts for development/test/build purposes but must not become runtime service locators or bypass product dependency direction. `oteryn-test-support` depends only on `foundation` and `diagnostics`; `oteryn-asset-compiler` depends only on `oteryn-asset-types` plus the exact workspace JSON parser; `oteryn-identity-security-tests` consumes only the W7 Identity/Platform and merged ENTRY contracts required for synthetic fake-service evidence; `oteryn-technical-login-integration-tests` consumes the exact merged W7 layers plus `app-runtime` for original fake-service composition only.',
          )
          replace(
              'oteryn-client/docs/operations/RUST_WORKSPACE.md',
              'The current `app` package depends directly on foundation/diagnostics, exact `winit 0.30.13` and the Windows-only `oteryn-renderer` composition contract; `oteryn-test-support` is dev-only. GPU ownership remains inside `oteryn-renderer`; the app must not absorb protocol, feature, persistence or direct Win32 responsibilities.',
              'The current `app` package depends directly on foundation/diagnostics, exact `winit 0.30.13`, the Windows-only renderer and the exact W7 application/Identity/Canary composition layers; `oteryn-test-support` is dev-only. GPU ownership remains inside `oteryn-renderer`; credential ownership remains inside producer lifecycle/adapter boundaries; the app must not absorb protocol parsing, feature state, persistence or direct Win32 responsibilities.',
          )

          Path('docs/agents/tasks/active/OTC2-20260731-w7-login-e2e.md').write_text('''---
task_id: OTC2-20260731-w7-login-e2e
project_lane: otclient-v2
policy_version: 2
task_kind: implementation
implementation_authorized: true
status: validating
agent: "W7-LOGIN-E2E worker"
track: greenfield-rust
workstream: application-runtime-technical-login
parallel_wave: OTERYN-W7-TECHNICAL-LOGIN
parallel_lane: W7-LOGIN-E2E
parallel_lane_state: validating
phase: validate
session_id: chat-github-20260731-w7-login-e2e-2
session_role: implementer
session_rotation_count: 1
execution_mode: chat-github
execution_reason: GitHub DNS and local Rust are unavailable; exact validation uses repository CI
lease_expires_at: 2026-07-31T19:30:00+02:00
branch: feat/OTC2-20260731-w7-login-e2e-final
base_branch: main
created: 2026-07-31T09:29:00+02:00
updated: 2026-07-31T18:35:00+02:00
last_verified_commit: "87ac50efc97b569712276666a30c8d671cc099b1"
required_base_commit: "a43cf375dc25b95df790e515ca222bc80cae26e1"
risk: high
related_pr: "#118"
depends_on:
  - W7 entry producer 9ecc43a4465f6565bc1c12ea61f170a96edcbe35
  - W7 Identity producer d66da47a33d6639876f3edda2b2c08709d1b7a5e
  - W7 Canary producer 4a193bdf10ac32a8a2d8dc12f31706c7d668c8f9
integration_after:
  - W7-ENTRY-CONTRACT
  - W7-IDENTITY
  - W7-CANARY-ENTRY
owned_paths:
  - oteryn-client/crates/app-runtime/**
  - oteryn-client/tests/integration/technical-login/**
  - oteryn-client/docs/research/technical-login/W7_LOGIN_E2E_EVIDENCE.md
  - oteryn-client/apps/client/**
  - docs/agents/tasks/active/OTC2-20260731-w7-login-e2e.md
shared_path_lease:
  - oteryn-client/Cargo.toml
  - oteryn-client/Cargo.lock
  - docs/agents/MODULE_CATALOG.md
  - docs/agents/BUILD_TEST_MATRIX.md
  - docs/agents/CHANGELOG.md
  - oteryn-client/docs/architecture/REPOSITORY_LAYOUT.md
  - oteryn-client/docs/operations/RUST_WORKSPACE.md
contract_role: final-consumer-composition-owner
contracts_produced: []
crates_touched:
  - app-runtime
  - client
  - technical-login integration tests
stale_takeover_count: 1
human_interruptions: 2
---

# Goal

Complete the bounded first Rust technical-login composition while preserving the existing Windows window/renderer/close lifecycle and every producer ownership boundary.

# Delivered

- one generation-checked Identity worker and one admission worker;
- explicit cancellation and join before lifecycle cleanup;
- producer-owned directory selection, credential handoff, failures and `SessionEntered`;
- original fake browser/listener/HTTP/admission end-to-end evidence;
- explicit environment-only technical configuration with no credentials/default endpoints;
- typed `winit` user events and bounded polling without network work on the event-loop thread;
- production Canary fail-closed behavior without compatibility claims.

# Validation checkpoint

```yaml
checkpoint_version: 1
updated_at: 2026-07-31T18:35:00+02:00
head: pending-final-doc-restack
branch: feat/OTC2-20260731-w7-login-e2e-final
pr: 118
status: validating
phase: validate
execution_mode: chat-github
last_completed_step: full feature-head Rust and repository gates passed before final documentation/restack
proven:
  - Rust Client run 30647012485 passed locked metadata, fmt, strict Clippy, all tests/doctests, architecture and cargo-deny
  - repository CI run 30647013322 passed
  - fake merged Identity to Gateway to runtime to ordered synthetic admission reaches SessionEntered
  - production Canary remains fail-closed before network and credential handoff
  - no temporary workflow remains in the intended final diff
unknown: []
blockers:
  - W7-BLOCK-REAL-RUST-E2E: exact deployed services/configuration/credential and controlled real admission unavailable
  - W7-BLOCK-DEPLOYMENT-EVIDENCE: deployed TLS/DNS/firewall/secret/runtime revision state unavailable
  - interactive Windows desktop observation unavailable; no runtime compatibility claim
next_action: Run exact-head Rust Client and repository CI after this restack/documentation commit, inspect complete diff/review threads, then mark ready and merge through branch protection.
```
''', encoding='utf-8')
          PY

      - name: Commit restack and evidence
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/agents/MODULE_CATALOG.md docs/agents/BUILD_TEST_MATRIX.md docs/agents/CHANGELOG.md docs/agents/tasks/active/OTC2-20260731-w7-login-e2e.md oteryn-client/docs/architecture/REPOSITORY_LAYOUT.md oteryn-client/docs/operations/RUST_WORKSPACE.md oteryn-client/docs/research/technical-login/W7_LOGIN_E2E_EVIDENCE.md
          git commit -m "docs(w7): finalize technical login evidence and restack"
          git push origin HEAD:feat/OTC2-20260731-w7-login-e2e-final

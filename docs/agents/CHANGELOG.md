# Agent-Facing Change Log

- 2026-09-02: Track A canonical exact-client fence advances to `15.32.be4f48` / `52105824` / `552dcf794c41dae8c3dca10b740cd23e2f2ebcaf82d86576e8a67d924409e4e1` from owner-authorized official-launcher manifest + exact installed-ELF agreement. Canonical session/transition/adoption/bootstrap identity, Kasm bootstrap and Phase 2 read-only admission are synchronized to the new tuple. Raw CDN refetch was blocked by Cloudflare challenge/HTTP 403 and is not counted as proof. Historical offsets, serializers, QMeta addresses and prior build-specific RE evidence remain fenced to their source builds; this repository-only advance grants no login, credentials, gameplay, GUI input, process-control, memory-access or semantic-promotion authority.
Curated behavior/architecture log for discovery; not a replacement for Git history/release notes.

## Unreleased
- 2026-09-02: Vision P2 trusted-composition integration PR #846 combines the frozen capture-edge, control-bridge and authenticated edge-transport semantics behind one application-owned fail-closed composition seam. Reviewed capture policy/root and runtime contracts are not caller/task/API/MCP/peer-selected; raw peer capture cannot self-assert secret safety; PNG masking is recomputed; capture is exact-admission identity bound; replay state is stored in the existing Control Center SQLite store and `load -> verify -> persist` is serialized in one `BEGIN IMMEDIATE` transaction. Exact code-generation Package A/B, Track A governance, browser/CLI E2E, fresh falsification and repository CI passed on `062a5c173b3410ec8fc2e5efaaefa1c4e34d15d6`. Repository/static authority remains `runtime_access:none`, mutation/login/gameplay false and physical budget/count `0/0`; live read-only observation remains a later serialized gate.
- 2026-08-31: Local-only Task 1–9 implementation on Draft PR #810 establishes a `repository_control_foundation`: the existing Control Center owns persistent agent session/provenance, bounded dashboard/API/CLI controls, fail-closed model-slot/reconciliation state, fake/offline evidence and the five-tool stdio MCP bridge; `tools.tibia_re_vision` reuses PR #790 safety primitives. Task 9 Architect Ruling C is independently re-reviewed **CLEAN** at `b5f83efae16203475a61e2887ad9952e435f84fd`. This is not autonomous Tibia/runtime/credential/gameplay readiness: production executor remains Null/unbound, `runtime_access:none`, CUA disabled, physical budget/count `0/0`; credentials, Synology/Molehill transport or deployment, Official-runtime transitions and Hermes remain separate/unchanged. Stale Draft PR #615 remains open because its replacement is unmerged.
- 2026-08-30: Track A PR #801 adds a repository-only Kasm-aware canonical `create_new` path plus a separate metadata-only `prior_boot_zero_client_invalidation_v1` recovery mode. The invalidation transition requires a prior-boot fail-closed exact-fenced registration, a newer current controller generation, repeated all-container zero-client proof, zero Tibia windows and stable Kasm boot/container identity before removing only the stale canonical registration; it never restores that prior-boot record after deletion. The subsequent `kasm-bootstrap` transition re-proves registration absence, launches exactly one already-present exact client in `otclient-track-a-kasmvnc`, binds PID/start/container/fence to repeated Kasm proof, commits `state: UNKNOWN` with `bootstrap_provenance: kasm_create_new_v1`, and can roll back only the exact created process. The owner-only physical workflow uses two distinct leases/tasks, a durable one-shot consumed marker, no credentials/GitHub token on the self-hosted live job, and remains unusable until trusted-main merge plus separate live admissions. No physical bootstrap, login, gameplay, process-memory observation or semantic promotion is claimed by this implementation PR.
- 2026-08-28: Track A canonical exact-client fence advances to `15.32.75d4a0` / `52105824` / `d1a16819cec7e40cfee39c099d4868d2eb2d7c1c942078eda105233b5688817a` from independently promoted exact-current evidence. The canonical session worker, transition/Gate-B controller, existing-runtime adoption probe, mandatory runtime-admission contract and normative governance are synchronized as one trusted-base identity contract. This repository-only repair grants no login, credentials, gameplay, input, process-control or semantic-promotion authority.
- 2026-08-25: Track A adds a distinct fail-closed canonical boot-epoch registration recovery lifecycle in PR #696. `canonical_boot_epoch_recovery` / `boot-epoch-registration-recovery` preserves PR #693 same-boot recovery, requires a fail-closed prior-boot adoption registration plus repeated current-boot singleton exact-fence and window/process proof under the current lease/flock, atomically increments registration generation, remains `UNKNOWN`, and rolls back only its exact committed record. Numeric PID reuse across boot epochs is not treated as continuity. No login, credentials, relog, restart, character selection, gameplay, input, movement or semantic promotion is authorized.
- 2026-08-24: Track A canonical stale-registration recovery merged in PR #693 as `f4b92d88e9623d8c10b349803fbd7d797bd588d7`. The new `canonical_recovery` admission class and `stale-registration-recovery` transition reuse the existing canonical lease/flock/state root/registration path, require a fail-closed adoption registration plus repeated complete singleton exact-fence proof, replace both stale PID and start identity only with preserved boot/container-name/display/remote-view continuity and a recomputed fresh fingerprint, atomically increment registration generation and roll back exact transaction-owned state on post-commit failure. Recovery is not rebind or Gate B, keeps semantic state `UNKNOWN`, grants no mutation authority and performs no login, credentials, gameplay, input or physical runtime action.
- 2026-08-24: Track A canonical adoption rebind repair merged in PR #689 as `955d0f72cb2500ce951a48ffcadb0b11ac8b3210`; it narrows rebind preflight to accept only the fail-closed evidence refresh `BRIDGE_3_OF_3_SEMANTICS_UNPROVEN -> NO_STRUCTURAL_BRIDGE` while every other registered adoption fact remains strictly matched, then persists the fresh `state_evidence` before the normal exact post-commit Gate B check. Stable adoption identity drift still fails closed; the repair is repository-only with `runtime_access:none` and grants no login/input/gameplay/process authority.
- 2026-08-23: TIBIA RE Control Center Package B local control plane merged in PR #666 as `1e9f0245b2c7a249dfd0fdc9c6f8bdda2e9aa5e5` from exact head `be8e5324cf8df0a62b3f37f43156723b859e7ed6`. It adds secured loopback Control API v1, fresh backend-epoch nonce, exact Host/Origin checks, durable RequestLedger/ControlState persistence with serialized shared-SQLite access, thin browser + CLI consumers and fake-only STOP/reset/restart-safe experiments. Exact-head Package A/Package B audits, real Chrome+CLI E2E and required repository CI passed; `OFFICIAL_CLIENT_ACCESS=NONE` throughout.
- 2026-08-21: TIBIA RE Control Center Package A / `control-core` merged in PR #628 as `13b3f02a07a176662d766352d9af39619775a73d` and is terminally archived by lifecycle closeout PR #649. The backend-only package provides deterministic Scenario/Execution/Adapter/Artifact/Comparison semantics, durable STOP/idempotency/budget fencing, privacy-at-construction hardening, 121 focused regression tests plus the 65 mandatory falsification inventory and fake one-step E2E. Final exact-head Package A, Track A governance and repository CI passed, and fresh Codex Spark review reported no major/material findings. `runtime_access:none`; Package B and later Control Center consumers remain separate.
- 2026-08-21: Surveyor v2 PR #636 adds an exact-current-build `TIBIA-RE-AUTH-SESSION` lifecycle typed reader and dedicated hosted Surveyor validation. It exact-fences both the official client and deployed Qt StateMachine library, validates the live `TGameClient + 0x8d0 -> TAuthenticationProcessController` relationship, exposes only the exact `QStateMachine::isRunning()`-equivalent boolean, reads `/proc/PID/mem` read-only, retains no credential/play-session payload, never claims `IN_GAME`, and changes repository-only missing-reader accounting from 10 to 9 with privacy PASS expected on the exact head.
- 2026-08-20: Surveyor v2 adds its first exact-current-build typed reader for `TIBIA-RE-PLAYER-STATE`: a fail-closed static resolver rediscovered current `TPlayerData` vtable/RTTI and `playerPosition` signed-i32 offsets on SHA `ed5469...`, deterministic tests cover ambiguity and real objdump formatting, and collect-all now reports 10 rather than 11 unimplemented typed readers. Runtime values remain `CANDIDATE_PENDING_CAUSAL_E2E` with semantic promotion disabled until post-merge owner-controlled movement differential proof.
- 2026-08-20: Track A invalidates BRIDGE_3_OF_3 as standalone gameplay-state authority after a fresh exact-peer login-screen regression still returned one validated player-protocol, game-session and worldmap handler. Surveyor now reports STRUCTURAL_IN_GAME=UNKNOWN and OWNER_LOGIN_REQUIRED=UNKNOWN; Kasm adoption records UNKNOWN; native-login bridge-only completion is INCONCLUSIVE. The final Surveyor bundle remains valid for collector/coverage/privacy/manifest evidence, but its prior in-game/login-required verdict fields are superseded.
- 2026-08-20: Repository-wide local-model execution now follows `LOCAL-MODEL-SINGLE-RESIDENCY-V1`: one physical host/shared GPU pool may have at most one resident or actively inferencing local model; model switches must unload and revalidate residency first, and unknown/multiple/different residency fails closed. This promotes the owner resource rule without merging blocked Ollama PoC PR #615 or granting any model/Track A/credential authority.
- 2026-08-20: Track A hardens Surveyor v2 runtime isolation in PR #623: the collector and trusted read-only operator stop enumerating the shared Docker host and verify only the declared `otclient-track-a-kasmvnc` namespace, exact singleton client PID/size/SHA, X11 ownership, canonical registration metadata and exact-peer bridge state. `target_uniqueness=PROVEN` is namespace-scoped; unrelated project containers are neither listed nor executed against.
- 2026-08-20: Track A adds Surveyor v2 collect-all in PR #616, reusing the Surveyor v1 foundation from stale Draft #592 on current main. The tool emits the canonical 169-row evidence index, allowlisted runtime provenance, eleven subsystem telemetry documents, all twelve TIBIA-RE alias views, a ranked `missing-readers.json`, deterministic privacy scan and `manifest.sha256`. Collect-all is passive/read-only, retains no raw character-bearing window titles or full process environment, cannot promote canonical semantic status, and leaves unavailable typed readers explicitly `UNKNOWN`/`UNAVAILABLE` for later gap-driven work.
- 2026-08-19: Track A promoted the PR #599 no-kill native login sequence into permanent workflow `.github/workflows/track-a-native-login.yml` plus `docs/agents/operators/TRACK_A_NATIVE_LOGIN.md`. The operator is owner-gated, exact-current-SHA fenced, uses GitHub Actions secrets only for one bounded native-auth step, preserves the authenticated replacement process after handoff, verifies helper `SO_PEERCRED`, fails closed unless `character_count == 1`, and dispatches only native `CONFIRM_UNIQUE`. Its original 3-of-3 bridge gameplay-success criterion is superseded by the 2026-08-20 false-positive correction above.
- 2026-08-19: Track A adds a fail-closed metadata-only `adopt-existing` canonical-live transition for the already-running/unregistered exact Kasm client case. It uses the current lease plus continuously held canonical flock, a read-only all-running-Docker-container Kasm probe, exact boot/PID/start/version/size/SHA/display/window proof repeated around atomic registration commit, title hashing, and registration-only rollback. Create-bootstrap stays zero-client-only; adoption performs no login/input/process mutation and does not authorize later GUI control without fresh trusted-main admission, Gate B and guarded mutation lifetime.
- 2026-08-19: Track A current official-Linux runtime identity fence advances to embedded version token `15.32`, size `52109920`, SHA-256 `ed5469b9fa71349de688f719434d23875f76f28a3ebd08a36d30f7f6da0af6b8` (public package SHA-256 `1fc26d66cef90723d29293f177fcff41c8e937e7aac830f08e82c2f4c69eb354`, ELF build ID `d803d9695868713ef6ab0c3cf65f91212c9c6a62`). Canonical admission/bootstrap/Gate-B enforcement and deterministic governance checks are synchronized. The prior `15.32.df7b29 / 51965216 / e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe` corpus remains historical evidence only; no old offsets/helpers/ABI assumptions are promoted, and this identity change grants no login, credential, input, gameplay, transaction or mutation authority.

- Added the repository-native agent orchestrator control plane in PR #463: deterministic bounded READY-task selection, dependency and owned-path serialization, standardized worker-result validation, bounded matrix fan-out/fan-in smoke, barrier recomputation and repository context-pressure rotation. The MVP worker executor remains simulated/dry-run only and invokes no Codex/OpenAI API, model credential or owner-funded AI quota; any real model executor remains a separately authorized adapter.
- Track A P1 now has a reusable exact-client-fenced runtime bridge/health contract in Draft PR #357: read-only local IPC/discovery is bound on every lifecycle connection to the explicitly admitted Linux peer with `SO_PEERCRED`, current boot identity, PID/process-start ticks and exact executable size/SHA, while `PING` carries a matching exact-runtime identity envelope. Discovery reports explicit `/proc` scan failures instead of collapsing them into successful zero hits; only a completed matching scan may return `scan_status=OK`. Health, stale rejection, reacquisition and recovery remain bounded, fail-closed and GitHub-hosted with `runtime_access: none`; `session-status` stays `DERIVED_UNTIL_LIVE_CORRELATION`. Activation through `launcher.py` uses invasive `LD_PRELOAD` process instrumentation and remains exclusively RUNTIME-owned; P1 does not claim current `:98`, `6082`, PID/session or physical E2E state.
- Historical v5 record (superseded by the current adoption entry above): Track A canonical-live policy v5 separates four fail-closed transitions on the final cancellation-safe manager: Gate A requires a current authoritative lease plus the PR #321 supervisor holding `coordination.lock` for the complete guarded mutation-tree lifetime even through foreground process-group cancellation; an older-generation but otherwise exact authoritative registration may cross to a newer controller generation only through a dedicated under-lock rebind that freshly proves unchanged boot/PID/start/exact-fence/display/window/state and target uniqueness before atomically incrementing `registration_generation` and binding `lease_generation` to the current controller; Gate B then requires that current-generation authoritative registration plus fresh exact-runtime preflight before ordinary reuse/mutation; and first creation remains the separate fail-closed bootstrap transaction rather than weakened reuse/rebind. The rebind/bootstrap transitions are governance contracts only and are not implemented or live-authorized by this PR. The exact client fence remains `15.32.df7b29 / 51965216 / e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe`; `:98`, `6082`, PID and session remain `UNKNOWN` / `NOT_REGISTERED` until direct evidence. Track B and PR #303 mutable runtime ownership remain isolated.
- Track A canonical-live controller authority comprises the PR #312 lease manager, PR #313 post-lock-time/killed-parent hardening, PR #317 normal-launcher last-close hardening, PR #316 out-of-band child-subreaper supervision and PR #321 cancellation hardening. Production `guard-run` acquires and validates under serialized POSIX `flock`, keeps the coordination descriptor exclusively in a dedicated Linux supervisor, starts the guarded command with `close_fds=True`, and retains serialization through the complete primary plus adopted/orphaned descendant lifetime. The supervisor now blocks normal cancellation signals across the fork setup window and installs non-terminating handlers before restoring the mask, so foreground process-group cancellation cannot release `coordination.lock` while a signal-ignoring guarded descendant survives; cancellation already pending before the supervisor exists is relayed fail-closed. Generic manager lock release still uses descriptor last-close semantics, and the guarded command never controls the flock descriptor. This remains cooperative same-UID programme governance only: it does not identify or register a canonical Tibia PID/session/display, does not make `:98` or `6082` canonical, and does not by itself satisfy PR #311's separate current-runtime identity gate.
- The greenfield Rust client now owns exact Tokio `1.51.4` at the application boundary and uses protocol-neutral bounded full-duplex TCP session supervision with separate gameplay/background/inbound queues, explicit total-operation connect/read/write/idle deadlines, control-priority cancellation, session-generation fencing, typed terminal framing/connection failures, TCP_NODELAY, bounded allocation, non-secret high-water/overflow metrics and deterministic joined shutdown. Identity, Gateway, Game Session and protocol-canary public/wire contracts remain unchanged; the prior blocking implementation is retained only as a feature-gated evidence baseline. Separate-process Windows loopback evidence recorded 40/74/91 Âµs blocking versus 42/70/99 Âµs Tokio p50/p95/p99, 3.82Ã— pipelined synthetic throughput, 0.75Ã— measured process CPU time, zero queue-full loss and 15.788 ms saturated-consumer cancellation without any physical-network latency claim.
- Post-W7 R1 secret-lifecycle hardening now moves raw PKCE entropy, bounded callback scratch/request/target data, decoded callback values, OAuth form bytes, the project-owned bearer-prefix precursor, Gateway JSON and producer secret DTO fields through project-owned owners that deterministically overwrite their visible bytes on terminal drop. Rejected secret inputs are overwritten before release and the verifier is moved without an extra ordinary string copy. The guarantee is deliberately limited: URL-library storage, HTTP/TLS adapter copies, allocator/compiler copies, process arguments, operating-system state and browser state remain external residual copies and are not claimed as erased.
- The greenfield Rust workspace now includes the W7 technical-login application composition: `oteryn-app-runtime` owns generation-tagged cancellable/joined Identity and admission workers and consumes only the merged ENTRY lifecycle; the existing Windows `winit` shell exposes one explicit environment-only configuration and handles typed completion events without network work on the event-loop thread. Original fake services prove dynamic callback, strict Gateway bootstrap, authoritative selection, one credential handoff, ordered synthetic admission through `0x0F`, fresh retry, cancellation/close and redaction. Production Canary remains fail-closed, and real deployment plus interactive desktop compatibility remain explicitly unproven.
- The greenfield Rust workspace now includes the bounded W7 Canary Current entry boundary: `oteryn-transport` owns one explicit-timeout, cancellation-aware, size-bounded non-reconnecting TCP connection; `oteryn-protocol-core` owns checked bounded binary helpers and closed non-secret parser errors; `oteryn-protocol-canary` consumes the merged one-shot ENTRY credential/lifecycle and exposes typed admission outcomes without raw sockets or credentials. Original synthetic tests prove malformed/truncated/oversized input, partial I/O, timeout, cancellation, abrupt close, wrong/expired/consumed/duplicate credentials, denial mapping, deterministic parsing and secret-free errors. Exact source evidence records Canary `95b276db311cf6e9acd58b847f1fb0ca6697b137`, while real RSA/XTEA/sequence/compression admission remains disabled before network/credential handoff until approved transcript/deployment evidence and one controlled Rust run through ordered `0x0F` exist; gameplay remains absent.
- The greenfield Rust workspace now includes the W7 native Identity consumer: `oteryn-platform` owns a strict bounded OAuth, Game Login Ticket and Gateway protocol-v1 HTTP/DTO boundary using platform-native certificate/hostname verification; `oteryn-identity` owns CSPRNG PKCE `S256`, a pre-bound OS-assigned dynamic IPv4 loopback callback, direct system-browser launch and generation/cancellation-safe one-attempt bootstrap. Raw producer data converts only into merged ENTRY contracts; passwords, fallback, embedded browser, async runtime, persistence, Canary packets and production defaults remain absent. Synthetic tests prove success and required callback/HTTP/directory negatives, while deployed client ID/URLs/TLS/network state and interactive Windows return remain explicitly blocked.
- The greenfield Rust workspace now includes the sole W7 shared entry-contract producer: `oteryn-account-session` owns a non-secret client-local `AccountSessionId`; `oteryn-world-directory` owns bounded signed-64 world/character identity, deterministic authoritative directory ordering and explicit validated selection; `oteryn-game-session` owns one-shot redacted credential handoff, typed lifecycle/failures/recovery actions and non-secret `SessionEntered`. It adds no OAuth/HTTP, browser, transport, Canary packet, UI, async runtime, adapter-specific field or external compatibility claim.
- The greenfield Rust workspace now includes bounded W6 synthetic asset foundations: `oteryn-asset-types` defines non-zero IDs, blob/RGBA8 metadata, SHA-256 digests and an original deterministic little-endian pack v1; `oteryn-asset-compiler` safely compiles strict JSON manifests plus relative original/synthetic source files with checked limits, path/symlink/containment/regular-file validation and immutable final-output semantics. It adds no asset runtime, renderer/GPU integration, real Tibia/Canary importer, proprietary bytes, updater/download, signing, protocol, UI or production compatibility/performance claim.
- The greenfield Rust workspace now includes bounded W5 renderer surface ownership: transactional typed CPU lifecycle, exact `wgpu 30.0.0` DX12 surface/device/queue ownership, exact `pollster 1.0.1` for one synchronous main-thread bootstrap and one event-driven constant clear/present path composed by the existing shell. It adds no game rendering, assets, shaders/pipelines, UI, protocol, direct Win32/unsafe, reusable async runtime, background service or continuous redraw loop; interactive GPU/driver/hardware/recovery/performance compatibility remains explicitly unproven.
- The greenfield Rust workspace includes the bounded `oteryn-client` Windows application shell: a deterministic typed lifecycle/command/window snapshot, bounded structured diagnostics, one main-thread `winit 0.30.13` window adapter and one joined proxy-wake thread. W5 composes the renderer through that existing lifecycle while keeping GPU ownership outside the app; direct Win32, unsafe, async runtime, protocol, identity, networking, assets, audio, feature UI and persistence remain absent, and interactive DPI/IME/input/minimum-Windows compatibility remains explicitly unproven.
- The greenfield Rust workspace now includes `oteryn-test-support`, a test-only `tool` crate that reuses the shared `ManualClock` for deterministic timelines and exact technical context, and builds bounded diagnostic fixtures from reviewed static message/field text plus already-classified values. It adds no second clock, sleep, scheduler, executor, async runtime, hidden thread, global registry, sink or product integration.
- The greenfield Rust workspace now includes `oteryn-diagnostics`, a bounded contract-only crate for stable severity/category/code values, reviewed static safe text, explicit sensitive classifications, redaction at diagnostic-value creation, deterministic technical context and at most 16 uniquely keyed event fields. It installs no logger/subscriber, sink, upload, crash-report, support-bundle, replay, async runtime or product integration, and arbitrary runtime text has no implicit safe conversion.
- The greenfield Rust workspace now includes the standard-library-only `oteryn-foundation` crate: non-interchangeable process/session/task generations with checked exhaustion, monotonic `Instant` and deterministic manual clocks, explicit deadlines, cancellation source/token ownership with documented Drop behavior, and closed primitive-specific non-secret errors. The architecture checker recognizes `foundation`, permits lower layers to depend on it and rejects foundation dependencies into product layers.
- The greenfield Oteryn Rust workspace bootstrap established a pinned toolchain, `oteryn-architecture-check`, locked dependency policy, unsafe-forbidden workspace lints, synthetic valid/invalid dependency graphs and additive Windows plus supply-chain CI. Product packages continue to be added only by their bounded work packages.
- The completed foundation audit records the minimum playable slice, Canary Current-profile candidate, Oteryn session/channel-routing gap, asset-rights and performance blockers, reproducible fixture/benchmark plans and the narrow workspace bootstrap decision.
- The target Oteryn client is a greenfield Rust product isolated under `oteryn-client/`; Canary is the first protocol adapter, Oteryn is the target ecosystem, gameplay channels are selected through login/relog, and production workspace bootstrap is gated by a documented foundation audit. The existing C++/Lua/OTUI client remains legacy/reference evidence and is not a Rust runtime dependency; its maintenance architecture and workstreams remain separately documented.
- The legacy options compatibility adapter now owns inventory-expiry state and delayed inventory reloads directly instead of temporarily replacing global `scheduleEvent`/`removeEvent`; generation checks suppress raced stale callbacks, only adapter-owned handles are cancelled, and the focused options Lua suite is registered as a separate CTest target.
- Deterministic option defects are repaired without exposing unsupported features: Right Bar 3 clears bar 9, reset IDs are unique, inventory expiry owns/cancels its reload event, delay labels match their actions, dead status/unused-expiry controls are hidden, cooldown-window wording is accurate, and `showExpiryInInventory` dual-reads/writes the legacy `showExpiryInInvetory` key.
- Forge now tracks every module-owned scheduled handle, cancels pending events before hide/game-end/terminate cleanup, releases callback references and uses generation guards so a raced stale callback cannot touch a reopened controller.
- Wheel of Destiny conviction summaries now use one named parser-order contract so skill, life leech, mana leech and five spell perks read slots 5-12 instead of the shifted legacy 3-10 assumptions.
- Action-bar cooldown protocol state is retained independently from visual options, subscribed for the module lifetime, reset at session boundaries and restored after relog/rebuild using the longest individual or spell-group remaining time for spells, runes and multi-actions.
- Character-list lifecycle now retains an absolute module-local OTUI path, safely recreates a destroyed legacy or Oteryn list from the existing login response, and returns to EnterGame instead of dereferencing a nil window when layout loading fails.
- Client-assets GitHub release resolution now selects archives by the release's own client-version evidence, excludes macOS and unrelated legacy archives, preserves non-archive metadata and forces the established codeload fallback when no match exists. Existing strict hashes, extraction and standard things/sounds/extras paths remain authoritative; production runtime compatibility still requires a clean real-release rehearsal.
- The reviewed `opentibiabr/otclient` synchronization retains bounded unknown-opcode recovery, pre-780 inventory use-with lookup, ground-border multi-use targeting, animator-driven always-animated creature phases, NPC trade imbuement accounting/lifecycle cleanup, `--user-dir`, Stats pause/resume, manual-walk/bot coordination, browser Lua/UIGraph/shader compatibility and Cocoa mouse delta handling.
- Two reviewed upstream effects remain deliberately deferred: Reward Wall source-byte semantics pending an exact Canary `OTS-*` contract, and rendering/preload ordering pending a framework-safe implementation without a framework-to-client dependency.
- Required CI compilation is temporarily Windows-only; Linux, macOS, Android, browser and Docker reusable workflows remain available but are not called by the primary workflow. Fast/static checks and Lua syntax remain required.
- Unix desktop browser URLs are now launched as exact process arguments instead of shell commands, preserving complete OAuth query strings and preventing shell metacharacter interpretation.
- Added persistent multi-agent coordination, autonomous PR/CI/merge rules, active-work discovery, module catalogue, task/handoff templates, ADRs, and cross-repository contracts.
- Protocol-game error callbacks now validate the exact source `ProtocolGame` before entering global `Game`, preventing delayed callbacks from obsolete sessions from disconnecting a replacement login.
- Deferred proxy and packet-player callbacks now retain explicit shared protocol ownership instead of asynchronously capturing raw `this`.
- Game connection-error, game-end and explicit logout paths now retain the exact source `ProtocolGame` through cleanup and revalidate identity after Lua-reentrant boundaries, so an obsolete session cannot disconnect its replacement.
- Deterministic game-lifecycle regression tests use a dedicated friend access seam instead of preprocessor access remapping, preserving MSVC link compatibility.
- Oteryn native authentication is active in PR #17: system-browser Authorization Code + PKCE, OS-assigned loopback callback, separate Platform Game Login Ticket issuance, standalone Game Gateway `/v1/login` consumption, server-authoritative world routing, and a one-shot `GameSessionKey` handoff consumed only after the actual `g_game.loginWorld` transfer without an Oteryn password fallback.
- The Oteryn login profile is disabled by default and production enablement remains blocked on the separately selected/proven Canary Game Session adapter and exact-version cross-repository E2E.

## 2026-07-12 bootstrap inventory

- Runtime Lua syntax checks are scoped to `data`, `modules`, and `mods` after merged PR #2.
- Reusable client unit/integration/Lua test foundation was merged in PR #3: message builders, fake resources/state, test environment, tile/thing builders, Lua runner/contracts, OTML fixtures, protocol loopback.
- Standalone agent handoff is active in PR #4 and must be reconciled with this system.
- Client-assets auto-install retains strict hashes and OTC-standard final paths.

# P0 RUNTIME discriminator contract — 2026-08-17

Consumer task: `OTC-20260815-track-a-p0-direct-position` / Draft PR #302  
Physical evidence owner: `OTC-20260816-track-a-canonical-runtime-e2e` / RUNTIME

## Admission prerequisite

Do not execute this discriminator until RUNTIME itself has freshly proven the exact official-client process/resource identity and uniqueness under the current trusted-base admission path. P0 does not transfer or acquire runtime ownership.

## Required physical evidence

A single bounded RUNTIME evidence package is sufficient if it contains all of the following:

1. exact official native-Linux client fence: version `15.32.df7b29`, size `51965216`, SHA-256 `e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe`;
2. fresh PID/start identity and direct proof that the observed X11/resource identity belongs to that exact process;
3. structural `IN_GAME` proof independent of a window merely existing;
4. at least two observations of the candidate direct player-position read with the independently decoded structural world coordinate captured at the same observation points;
5. negative controls sufficient to reject viewport center, map origin, camera position and stale/copied position values;
6. if a movement stimulus is needed, exactly one safe adjacent reversible step plus inverse restoration, with structural before/after/restored state;
7. repeat after fresh PID/relogin if that lifecycle is already part of the admitted RUNTIME phase.

## Promotion rule

P0 may promote direct authoritative XYZ only if the candidate read agrees with independent structural world state through the required observations and controls. A function call, emitted signal, socket bytes, window motion, viewport change or one static field-shaped triple is insufficient.

## Current status

At creation of this contract, hosted raw-XRes helper source PR #447 had passed deterministic validation and coordinator promotion PR #448 was open. Physical identity retry was still forbidden until promotion and fresh separate RUNTIME admission.

No physical runtime action is authorized by this document.
# Track A P2 — first concrete retained-writer serialization boundary

Date: 2026-08-15  
Task: `OTC-20260815-track-a-p2-first-transform-boundary`  
Disposition: **DRAFT / NOT PROMOTED**

## Result

`SERIALIZATION_ONLY_PROVEN`

For the exact official native Linux Tibia client, the retained branch is independently reproduced as:

```text
TProtocolClientMessageProcessor
  -> retained intermediate object (AP 0x2f69e30, RTTI 0x3080748)
  -> retained shared TProtocolWriter object (AP 0x2f69dd0, RTTI 0x3080728)
```

The first two intermediate vtable slots are lifecycle-like (`0x7de7f0`, `0x7dfd60`). The first concrete non-lifecycle slot is `0xc10960` at relative slot `+0x10`.

At `0xc10960`, exact disassembly shows:

- retained writer access through intermediate member `+0x18`;
- a message-derived dispatch value loaded through the argument object's virtual access path;
- serialization via `QDataStream::operator<<(signed char)`.

The next concrete slot `0xc20290` preserves the argument pointer (`rbx <- rsi`), reads structured fields at argument `+0x30` and `+0x34`, and serializes them via `QDataStream::operator<<(signed short)`.

An adjacent slot `0xc20c70` constructs `QBuffer`, but **vtable adjacency does not establish temporal pipeline order**. Therefore no ordering claim between `QBuffer` construction and the proven QDataStream serialization is promoted.

## Exact execution provenance

- workflow run: `31893080162`
- job: `95032159933`
- head: `f471bfc0b67046bdd917ea6e10a2e22af7f8d00f`
- runner: `synology-otclient-01`
- artifact: `9249061176`
- artifact digest: `sha256:2604ddaddd7381de0797ccfdc1c027ac49f66175485012647a4804a98e100130`
- workflow semantic execution: `SUCCESS`

Exact client fence verified by the executing job:

```text
version_mapping=15.32.df7b29
size=51965216
sha256=e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe
platform=official_native_linux_only
```

The artifact contains only `result.json`, `result.txt`, `validation.log`, and sanitized text disassembly evidence; no proprietary client bytes are uploaded.

## Independent reconstruction gates

The reproducer does not inherit #301/#305 conclusions blindly. It revalidates against the exact binary:

1. writer vtable preamble and RTTI at AP `0x2f69dd0` / `0x3080728`;
2. distinct intermediate preamble and RTTI at AP `0x2f69e30` / `0x3080748`;
3. intermediate slots `0x7de7f0`, `0x7dfd60`, `0xc10960`, `0xc20290`, `0xc20c70` as exact executable entries;
4. writer allocation/control setup at `0x1970d26..0x1970d6d`;
5. intermediate allocation and writer retention at `0x1970edd..0x1970f43`;
6. `TProtocolClientMessageProcessor` retention of the intermediate shared pair at `0x1971056..0x1971076`;
7. concrete QDataStream calls and argument provenance in `0xc10960` and `0xc20290`;
8. lifecycle negative controls for the first two slots;
9. superseded sink addresses are not used as proof.

## Proven representation boundary

### FACT

```text
structured/typed object argument
  -> retained TProtocolWriter-associated QDataStream serialization sink
```

The exact first concrete non-lifecycle operation is byte serialization (`signed char`), followed by independently proven structured 16-bit field serialization in the next slot.

This closes a concrete serialization boundary on the retained writer branch. It does **not** prove that this slot is the temporally first transform in the entire outbound pipeline.

## Explicit UNKNOWN boundary

The following remain `UNKNOWN` and must not be inferred from this result:

- temporal first operation in the complete outbound pipeline;
- framing order;
- sequence-number order;
- compression boundary or order;
- encryption boundary or order;
- `QBuffer` ordering relative to the proven serializer calls;
- final binary egress / socket ownership;
- a causal local harness connecting generated bytes to final network output.

Direct DualConnection writer ownership also remains `NOT_PROVEN`; this evidence concerns the processor-retained intermediate -> writer branch only.

## Negative controls

Not used as discriminator or promotion basis:

- generic `QIODevice::write` enumeration;
- generic Qt/QMeta census;
- vtable adjacency alone;
- final-socket run `31825417040`;
- superseded `0xb5b880`, `0xb46bd0`, `0xc33259` sink models;
- stale writer RTTI `0x3080700`.

## Safety / side effects

Static exact-build analysis only. No client launch, login, attach, credential access, packet replay, movement, gameplay, account state or network mutation occurred.

## Coordinator handoff boundary

Safe promotion is bounded to `SERIALIZATION_ONLY_PROVEN` plus the exact retention/call/data facts above. Any claim about framing, compression, encryption, final egress or end-to-end harness requires a separate discriminator.

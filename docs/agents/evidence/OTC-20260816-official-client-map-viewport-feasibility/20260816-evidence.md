# 2026-08-16 — official-client map viewport feasibility evidence

This file preserves only compact, non-proprietary evidence needed to continue the viewport-size investigation. It does not contain the Tibia executable, assets, credentials, private captures or transient runtime addresses.

## Exact researched client fence

Source: `docs/agents/reports/OTCLIENT-20260813-tibia-re-canonical-state.md`.

```yaml
client_version: 15.32.df7b29
client_size: 51965216
client_sha256: e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe
platform: official_native_linux_only
```

The canonical report also retains the version-fenced common-map static capture lead:

```text
0x19a8ea3
```

Historical PID, PIE base and runtime breakpoint addresses are intentionally not promoted here.

## Exact-binary static artifact

```yaml
workflow_run: 31892019505
workflow: Track A P0 direct player position
run_head: a3068a6a9460525cb1946186cf439caf7832e176
run_result: success
artifact_id: 9248797952
artifact_name: track-a-p0-static-elf-31892019505
artifact_digest: sha256:04835ab0bac7ffc43e161e8b2118c90a3d2197f7011385a6758cd7706c93a584
artifact_created_at: 2026-08-15T15:12:19Z
```

The artifact was downloaded and inspected during this task. The following semantic strings were present in its text outputs.

### Worldmap extent/storage/viewport/camera/protocol/render surfaces

From `static-elf-re.txt`:

```text
file_offset=0x1c8fee0  N5tibia8worldmap15TWorldMapExtentE
file_offset=0x1c9fe20  N5tibia8worldmap23TWorldMapSubfieldExtentE
file_offset=0x1ca9180  ...tibia8worldmap16TWorldMapStorage...
file_offset=0x1cabb60  ...tibia8worldmap17TWorldMapViewport...
file_offset=0x1cabce0  N5tibia8renderer15TWorldMapCameraE
file_offset=0x1cd59a0  N5tibia8worldmap31TWorldmapProtocolMessageHandlerE
file_offset=0x1cd67a0  tibia::worldmap::TWorldmapProtocolMessageHandler
file_offset=0x1cd9ad7  tibia::worldmap::TWorldMapExtentX
file_offset=0x1cdb580  ...tibia8worldmap23TWorldMapRenderProvider...
file_offset=0x1cddd20  N5tibia8worldmap23TWorldMapRenderProviderE
file_offset=0x1ce1b60  N5tibia8worldmap17TWorldMapViewportE
file_offset=0x1ce1c00  N5tibia8worldmap16TWorldMapStorageE
```

From `player-position-graph.txt`, in the local semantic-string window around the player-position graph:

```text
file_offset=0x1d2a44c  tibia::minimap::TMinimapRenderInfoStorage
file_offset=0x1d2a48a  onCameraViewportChanged
file_offset=0x1d2a4f5  onPlayerPositionChanged
file_offset=0x1d2a95d  tibia::worldmap::TWorldMapCoordinate
file_offset=0x1d2a9ae  TMapScaleFactor
file_offset=0x1d2a9be  MapScaleFactor
```

### Claim strength

```yaml
PROVEN:
  - these names/strings are present in the exact-binary static artifact
  - the exact client contains distinct named worldmap extent, subfield-extent, storage, viewport, camera, protocol-handler and render-provider concepts
  - camera-viewport and map-scale semantic names are present in the analyzed local graph
NOT_PROVEN:
  - exact object layouts
  - field offsets holding viewport width/height
  - constructor/default values
  - allocation strategy or absence of fixed-size arrays
  - call order between protocol, storage, viewport, camera and renderer
  - a supported public/configurable larger viewport
  - any particular patch point
```

The type/name separation is therefore evidence for architecture discovery, not proof that changing one integer safely expands the map.

## Historical structural movement evidence

```yaml
workflow_run: 31806312967
workflow: Track A persistent observer reversible step
run_head: ff8ebc6e2c3a1604d90c2b0439b60af2258b578a
job: 94785974126
job_name: probe
run_result: success
runner: synology-otclient-01
artifact_id: 9221332209
artifact_name: track-a-persistent-reversible-step
artifact_digest: sha256:bd4be5b2f9d6cebf19fff6bdfa3677ad57c00ae4a376987f32b42e8a27907a4a
```

At that exact historical head, `.github/workflows/tibia-official-client-re-persistent-reversible-step.yml`:

- SHA-fenced the executable to the researched client;
- reused the persistent structural observer;
- recorded event and strip line counts before, after one `Up` input and after the inverse `Down` input;
- printed all newly appended `map-provenance-persistent-events.tsv` and `map-provenance-persistent-strips.tsv` rows to the job log;
- uploaded three XWD frames (`before`, `forward`, `inverse`).

The retained artifact contains only the XWD frames, not the TSV rows/job log.

### 18 x 14 interpretation observed during the 2026-08-16 investigation

During inspection of the successful job output, the research session observed:

```text
TRACK_A_STRIP_COUNTS=0,33,88
```

and, within one `z=7` edge sequence, consecutive X coordinates:

```text
32537 .. 32554
```

which is 18 positions inclusive. The corresponding opposite-edge observation was separated by 14 Y positions.

Evidence classification for this numeric interpretation:

```yaml
status: DERIVED_FROM_OBSERVED_JOB_LOG
confidence: high
baseline_interpretation: 18_x_14
reason: one edge contained 18 consecutive coordinates and the orthogonal edge separation matched 14
promotion_limit: the raw TSV/job log is not preserved in the downloadable artifact consumed by this task
required_before_mutation_claim: reproduce or durably capture the raw rows under current governance
```

Do not use `33` or `88` alone as viewport dimensions. Those are cumulative strip-row counts across movement/floors/order and are not a width/height encoding.

## Quantitative candidate sizes

The tile count per floor is `width * height`; doubling both linear dimensions quadruples tile count.

| Candidate | Tiles/floor | Relative to 18x14 |
|---|---:|---:|
| `18 x 14` | 252 | `1.00x` |
| `26 x 20` | 520 | `2.06x` |
| `32 x 24` | 768 | `3.05x` |
| `36 x 28` | 1008 | `4.00x` |

These are experiment targets, not proven supported configurations.

## Feasibility conclusion preserved from the investigation

```yaml
FACT:
  - exact official client has distinct named extent/storage/viewport/camera/protocol/render worldmap surfaces
  - exact binary has camera-viewport and map-scale semantic names
  - historical structural movement observer captured map-strip updates on the exact researched client
DERIVED_HIGH_CONFIDENCE:
  - a larger loaded/rendered map area is technically plausible and merits targeted binary analysis
  - terrain/cache/render expansion should be investigated separately from full live dynamic-entity awareness
UNKNOWN:
  - whether one or several constants/fields control current dimensions
  - maximum safe dimensions
  - whether buffers/containers are fully dynamic
  - protocol parser/serializer assumptions and server-side map-awareness requirements
  - renderer/camera clipping, culling and coordinate limits
  - memory/CPU/GPU/network scaling on larger dimensions
  - whether static terrain can be rendered beyond live dynamic awareness without additional server data
NOT_PROVEN:
  - that 26x20, 32x24 or 36x28 works
  - that a one-byte/one-constant patch is sufficient
  - that the official server will supply a larger live area
```

## Required next research step

Static-first, no live mutation:

1. recover xrefs/constructors for `TWorldMapExtent`, `TWorldMapSubfieldExtent` and `TWorldMapViewport`;
2. identify candidate width/height fields and every writer/reader;
3. trace consumers into `TWorldMapStorage`, `TWorldmapProtocolMessageHandler`, `TWorldMapRenderProvider` and `TWorldMapCamera`;
4. search for fixed-size allocations, masks, loop bounds, clipping/culling constants and serializer/parser row/floor assumptions;
5. produce a patch-point dependency graph before changing any client bytes;
6. only then design the smallest mutation (`+1`/small expansion before `26x20`) under the current Track A runtime-admission, ownership and exact-SHA gates;
7. validate login/world entry, all four movement edges, floor changes, teleport/reposition, creature/object/effect updates and reversal before escalating dimensions.

No live runtime action is authorized by this evidence checkpoint.
# Pre-run status

```text
RESULT=DRAFT_STATIC_ONLY
FORM_UI_CONTROL=FORBIDDEN
NATIVE_COLD_AUTH_TARGET=TGameClient::onRequestLoginWithCredentials(QString,QString)
RUNTIME_ACCESS=none
CLIENT_EXECUTED=false
SYNLOGY_RUNTIME_TOUCHED=false
CREDENTIALS_ACCESSED=false
```

The proving workflow must fail closed unless it independently reproduces the exact packed/unpacked client SHA, QMeta class identity, unique method metadata entry, and unique executable dispatch target.

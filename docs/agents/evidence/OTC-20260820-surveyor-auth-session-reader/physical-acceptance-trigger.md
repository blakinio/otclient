# Physical acceptance trigger

This file exists only to create one same-repository owner-controlled pull request event for the trusted-base `pull_request_target` Surveyor auth/session read-only acceptance workflow.

It carries no runtime data, credentials, secrets, gameplay input, process-control authority, or mutation authority. The trusted workflow must ignore PR code and checkout `main` only.

# OTCLIENT-TIBIA-RE monster spawn/mechanics alias

```yaml
alias_prompt_contract_version: 1.0.0
alias: OTCLIENT-TIBIA-RE-MONSTER-SPAWN-MECHANICS
repository: blakinio/otclient
track_id: official-client-re
canonical_prompt: docs/agents/prompts/OTCLIENT_TIBIA_RE_MONSTER_SPAWN_MECHANICS.md
run_scope: autonomous_program
continuation_policy: continue_until_real_stop
```

Resolve this alias only from live `blakinio/otclient` state.

Read the canonical prompt above in full, then refresh current `main`, active tasks, open PRs, ownership, current official-client identity and Track A runtime admission state before selecting work.

This alias grants no runtime authority, login/session budget, credential access, client mutation, second logged-in session, external-repository write or direct Codex/OpenAI API authorization.

Owner invocation:

```text
Kontynuuj OTCLIENT-TIBIA-RE-MONSTER-SPAWN-MECHANICS autonomicznie.
```

The coordinator should continue safe GitHub-hosted validator/inference/resolver work even when physical `IN_GAME` evidence is unavailable, and serialize all physical evidence requests through a separately admitted Track A RUNTIME task.

# OTCLIENT-TIBIA-RE Surveyor v2 collect-all alias

```yaml
alias_prompt_contract_version: 1.0.0
alias: OTCLIENT-TIBIA-RE-SURVEYOR-V2-COLLECT-ALL
owner_short_alias: TIBIA-RE-SURVEYOR-V2-COLLECT-ALL
repository: blakinio/otclient
track_id: official-client-re
canonical_prompt: docs/agents/prompts/OTCLIENT_TIBIA_RE_SURVEYOR_V2_COLLECT_ALL.md
run_scope: autonomous_program
continuation_policy: continue_until_real_stop
task_completion_policy: finalize_archive_and_continue
```

Resolve this alias only from current live `blakinio/otclient` state.

Read the canonical prompt above in full, then refresh current `main`, active tasks, open Track A PRs, ownership, exact official-client identity, canonical runtime registration/lease/admission and the current state of any Surveyor/adoption work before selecting a phase.

This alias does not grant credentials, login/relogin, character selection, GUI/gameplay input, process control, attach/injection, process-memory writes, client-byte mutation, network mutation, economic/item transactions, a second logged-in session or direct owner-funded Codex/OpenAI use.

The owner performs any required login manually. Do not request that login until the canonical prompt's `COLLECTOR_READY=YES` gate is proven. If a legitimate exact current in-game canonical session already exists, reuse it and do not request another login.

Owner invocations:

```text
Kontynuuj autonomicznie TIBIA-RE-SURVEYOR-V2-COLLECT-ALL.
```

or:

```text
Kontynuuj autonomicznie OTCLIENT-TIBIA-RE-SURVEYOR-V2-COLLECT-ALL.
```

When a previous invocation stopped with `OWNER_LOGIN_REQUIRED=YES`, the owner may manually enter one character in the designated official Linux client and then resume with:

```text
READY — kontynuuj TIBIA-RE-SURVEYOR-V2-COLLECT-ALL.
```

`READY` is permission to resume the bounded programme after manual owner login; it is not permission to read credentials, log in another session, send gameplay input or weaken current Track A runtime gates.

# OTC-BE4F48-QT-CONNECTIMPL-RECEIVER-DELEGATION

Autonomous bounded Track A source-only successor after promotions #905/#908 and archives #906/#909. Refresh protected main, AGENTS, tracks, short commands, active tasks/open PRs, PR284 exact head/checks, current fence and runtime admission. Do not duplicate any live/consumed task.

## Exact source and proof boundary

Primary executable fence: read formal manifest; currently15.32.be4f48 /52105824 /552dcf794c41dae8c3dca10b740cd23e2f2ebcaf82d86576e8a67d924409e4e1. No address or ABI transfer from another build.

Promoted fact: selected connectImpl@0x7c6b9f has formal receiver rcx (hidden-sret ABI). Exact imported symbol:

`_ZN7QObject11connectImplEPKS_PPvS1_S3_PN9QtPrivate15QSlotObjectBaseEN2Qt14ConnectionTypeEPKiPK11QMetaObject`

Conditional QSlot operation1 dispatch at0x7c4f10 forwards entryrdx to adapter0xbd3050 rdi. External registered-receiver-to-QSlot-entryrdx binding remains NOT_PROVEN.

## Single objective

Qualify only the exact QtCore dependency shipped in the same public package and the first unique receiver-carrying delegation in that selected QObject::connectImpl implementation. First prove package membership, unique dependency identity, executable DT_NEEDED relationship, version/size/SHA for both files, and unique defined dynamic symbol. Do not treat package membership as proof of the library loaded by any runtime process.

Read the public package manifest only for exact bin/client and uniquely named libQt6Core.so dependency rows. Reject ambiguity or non-current executable fence. Materialize only those two transient files; do not enumerate or download unrelated libraries. Record sanitized dependency path/size/hash and exact selected symbol/FDE. Follow at most one unique receiver argument delegation edge from that FDE. Preserve conditional ABI and all unknown registration/storage/delivery semantics beyond that edge. No QObject/connect/QSlot/vtable/RTTI/global census. No historical/system Qt fallback or generic Qt docs used as exact deployed authority.

If the package does not contain a unique admissible QtCore member, emit precise SOURCE_BLOCKER; absence is package-scoped, not static-proof exhaustion. A unique new callee is only a bounded delegation fact, not complete Qt receiver binding or class identity.

## TDD, qualification, lifecycle

Dedicated task/branch/Draft PR from fresh trusted main. Repository-only behavioral RED before acquisition, minimal GREEN, exact-source qualification, deterministic sanitized JSON, independent whole-diff falsification and exact-head focused/CI/governance/boundary. Delete all transient raw client/dependency/package helper state before artifacts (sanitized result only). Source stays Draft and never self-merges. Clean docs-only promotion; close source unmerged as consumed; separate archive with ownership release; only then successor.

## Budget

This is the third sequential source task in the owner's explicit login-closure programme. The task record must declare max_additional_tasks_after_terminal_entry_task: 4 with reason: explicit ordered multi-task owner programme, bounded to original120-minute invocation clock and measurable proof progress. This expands the default task-count budget under the budget contract's explicit-record rule; it does not reset invocation_started_at2026-09-04T22:06:00Z, checks, repairs or runtime authority. Do not start if fewer than30minutes remain.

## Safety and unknowns

runtime_access=none; official_client_executed=false; login_performed=false; credentials_used=false; process_memory_access=false; packet_capture=false; ocr_vision_used=false; official_service_e2e_count=0; track_b_pr_284_modified=false. No client, loader, ldd, runtime process, login or official-service invocation. Static ELF parsing only. Receiver identity, complete sendLogin causal binding, final writer, Field6, pre-success order and Track B wire delta remain unproven unless separately promoted exact proof establishes them.

Task-local outcomes: bounded positive delegation fact or SOURCE_BLOCKER with FIRST_MISSING_BOUNDARY. ANALYSIS_INCOMPLETE is a nonterminal tooling checkpoint, never a scientific blocker.

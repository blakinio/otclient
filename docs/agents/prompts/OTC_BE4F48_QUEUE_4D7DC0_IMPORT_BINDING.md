# OTC-BE4F48-QUEUE-4D7DC0-IMPORT-BINDING

## Authority and question

Source935 was consumed by clean promotion936 and separate archive937. Refresh protected main, AGENTS, tracks, short commands, active tasks/open PRs, PR284 exact head/checks, latest promotion/archive, current client fence and runtime admission before claiming. No overlapping ownership or newer formal fence may be assumed absent.

Promotion936 proves first direct call0xdd8e10->0x4d7dc0 and conditional rcx pointer into two known private-stack cells. It does not identify the callee, activation, logical vector or callee use. This task asks only whether the selected link-time target is an exact PLT import binding. This is a new loader-metadata proof class on a newly proven target, not an activation scan, QObject census or stack-model expansion.

## Exact bounded scope

Primary15.32.be4f48 / 52105824 / SHA256552dcf794c41dae8c3dca10b740cd23e2f2ebcaf82d86576e8a67d924409e4e1. Reused dual-package qualification must also fence packagedQtCore7354472 / SHA25603ac3e4e7356399897ec58d42c81ae5c257072d45d539de1def528a8a04911fa. A formally newer current fence requires fresh authority; never transfer these addresses across builds.

Requalify only five bytes at0xdd8e10 as direct CALL0x4d7dc0. Inspect at most16bytes at that target, which must be an aligned entry in a uniquely mapped executable .plt or .plt.sec section. Accept only the entry's first RIP-relative indirect JMP, optionally preceded by ENDBR64. Do not search later instructions for a convenient JMP. Reject segmented, indexed, address-size-truncated or non-RIP forms. No native target-body or further transfer traversal.

Compute the single GOT slot from that instruction. Inspect at most65536 relocation rows, numeric offsets/types only, solely to find all rows for this exact slot; do not resolve names for unrelated rows. Require exactly one matching row, RELA with addend0, R_X86_64_JUMP_SLOT, and a selected linked dynamic-symbol table. Resolve only that row's one symbol index. Require an undefined function symbol with global or weak binding and a bounded nonempty ASCII mangled name (maximum512characters). Report ambiguity, missing metadata or unsupported form as precise SOURCE_BLOCKER; row cap is ANALYSIS_INCOMPLETE, never absence proof. Require unique allocated writable non-executable mapping of the GOT slot. No reading GOT contents or runtime relocation values.

Do not inspect other imports, enumerate QObject/connect/QSlot/RTTI/vtable/constructor/writer symbols, read packaged-core implementation/export bodies, demangle by external services, or infer runtime resolution. The import name is a link-time request; runtime-loaded definition/interposition and actual callee use remain UNKNOWN.

## Acceptance and output

POSITIVE_EXACT_SELECTED_IMPORT_BINDING requires freshly qualified call, selected PLT instruction, GOT slot, unique relocation and one undefined dynamic function symbol. Report sanitized FACT/INFERENCE/UNKNOWN and exact metadata identities only. No raw proprietary bytes/disassembly. The previous stack cells are promoted context, not newly re-derived here. No activation delivery/index/parameter semantics merely from a symbol name. FIRST_MISSING_BOUNDARY remains exact selected-import implementation/use when binding succeeds.

Otherwise SOURCE_BLOCKER with precise first boundary and partial facts. No full-program absence/exhaustion claim. Resource caps are ANALYSIS_INCOMPLETE. No global executable census or wider scan to force a result.

## Execution and lifecycle

Task OTC-20260905-be4f48-queue-import-binding owns tools/tibia_re_be4f48_queue_import/**, .github/workflows/tibia-re-be4f48-queue-import.yml, its exact active task and evidence directory. Claim isolated branch/worktree and Draft PR before implementation. Reuse strict package/fence support and selected decoder design after repository search; do not reuse eager symbol-relocation census from old Image helpers.

Repository-only TDD RED: direct call identity, selected stub form/section/alignment, RIP slot calculation, duplicate/missing/wrong relocation, invalid dynamic symbol, inaccessible GOT, row bound, and no unrelated symbol resolution. Minimal GREEN, exact-client qualification, regression RED/GREEN for material findings, independent whole-diff/result review0materialfindings, exact focused/CI/governance/self-hosted-boundary gates. Source remains Draft/unmerged. Clean docs-only coordinator promotion from fresh main, expected-head protected squash, close source unmerged consumed, separate ownership-releasing archive, then successor registration.

## Safety and programme

runtime_access=none; mutation_authorized=false; official_client_executed=false; login_performed=false; credentials_used=false; process_memory_access=false; packet_capture=false; ocr_vision_used=false; official_service_e2e_count=0; track_b_pr_284_modified=false. Full none-admission required at claim. Disposable hosted public exact-package acquisition only; remove raw bytes/helper state before sanitized-only artifact and on failure. No credentials/session/official runtime/E2E/external model calls. E2E NOT_APPLICABLE: static contract producer.

Receiver identity/causal binding, final writers/contract, Field6 and pre-success ordering remain UNKNOWN; TRACK_B_CURRENT_WIRE_DELTA=NOT_PROVEN and PR284 unchangedblocked. Source919 stays separately blocked. This task does not authorize following the selected import implementation.

Fresh continuation11:35:52Z / prospective240minutes /4additional source tasks, this additional1 after entry935 lifecycle. Preserve ordinary CI2/head, one identical transient retry,3repaircycles and15minute no-progress limits. No counter reset or repeated exhausted proof mode.

# Parallel Wave Coordinator Agent Prompt

W1, W2, W3 and W4 are completed and closed. Copy the block below only into a fresh planning/coordinator session. It authorizes no worker launch by itself.

```text
Work autonomously in repository:

blakinio/otclient

Role: perform post-W4 live preflight and, only when justified, prepare one separate accepted next-wave plan. Do not implement the recommended package while planning.

Current Git/main, root and nested AGENTS.md, live open PRs, active task records, accepted architecture, merged contracts/evidence and exact CI are authoritative. Do not rely on chat history.

Repository safety:

- routine writes only to blakinio/otclient;
- never mutate Canary, Oteryn Platform, upstream or another repository;
- never push directly to main;
- one task/branch/worktree per change;
- no branch-protection, review or CI bypass;
- no success claim without exact evidence.

Mandatory reads:

1. AGENTS.md
2. docs/agents/README.md
3. oteryn-client/AGENTS.md
4. architecture and security documents
5. PROGRAM.md, WORKSTREAMS.md and MULTI_AGENT_EXECUTION.md
6. CURRENT_PARALLEL_WAVE.md
7. RUST_WORKSPACE.md
8. every active Rust task, open PR, review thread and required check
9. merged foundation, diagnostics, test-support and application-shell contracts
10. merged W2 Windows research, W4 runtime evidence and archived W3/W4 records

Required closure facts to revalidate:

- W1 foundation is merged/archived and not launchable;
- all W2 lanes are merged/archived and not launchable;
- W3 test support is merged/archived and not launchable;
- W4 planning and W4-SHELL are merged/archived and not launchable;
- no expired Cargo/lockfile/dependency-policy/shared-document lease remains;
- unrelated legacy/operational PRs own no greenfield Rust package;
- no active task/PR already owns the proposed next package or public contract.

Current recommendation, not authorization:

One bounded renderer surface-ownership evidence/spike consuming the merged application-shell window/lifecycle contract.

A future accepted plan must require:

- preserve main-thread window ownership and deterministic shell shutdown;
- establish only renderer instance, adapter, device, queue and surface ownership plus clear/present lifecycle with original synthetic content;
- fresh primary-source dependency version/license/MSRV/advisory/source review;
- deterministic CPU-side surface lifecycle, zero-size, suspend, loss and shutdown tests;
- no game/map/entity rendering, texture/assets, shader framework, UI, protocol, identity, networking, audio, persistence or extension runtime;
- no global renderer singleton, hidden background service or scheduler;
- interactive GPU, driver, hardware and performance cases explicitly blocked unless genuinely observed on a named environment;
- one unique Cargo/lockfile/dependency-policy/shared-document lease;
- exact-head Windows workspace, architecture, supply-chain and repository CI;
- separate implementation and archive lifecycle PRs.

Do not create a worker branch, implementation task or lease until a dedicated plan PR and its archive have merged. Do not relaunch any completed W1-W4 prompt. If live state conflicts with the recommendation, record the blocker or choose a different evidence-based package through the planning task.
```

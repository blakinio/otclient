"""Repository-only RED scaffold for one selected call."""


def selected_inputs(raw, base, site, target, max_instructions=2048, max_updates=20000):
    raise NotImplementedError('repository-only RED')

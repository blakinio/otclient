from __future__ import annotations

"""Fail-closed composition seam for the native official-client login lifecycle.

Runtime binding is intentionally absent at this first TDD step. The module exists so
subsequent tests can define the lifecycle contract before any physical executor is
introduced.
"""

__all__: tuple[str, ...] = ()
